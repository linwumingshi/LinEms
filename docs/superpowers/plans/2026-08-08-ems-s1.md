# EMS S1 策略配置结构化 JSON 表单 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 PEAK_VALLEY 策略提供结构化配置表单（充/放窗口表 + 功率上限），窗口即时校验（start<end、powerLimit>0），保存前 JSON 语法 + 结构化双校验阻断；保留「切换 JSON 模式」兜底与格式化；其余 4 类型保持 JSON 文本域（仅语法校验）。

**Architecture:** 纯前端迭代。新增 `strategyConfig.ts` 纯逻辑模块（解析/校验/序列化，可单测钉死 schema 契约）+ 独立子组件 `StrategyConfigEditor.vue`（结构化表单 + JSON 模式切换 + 内联校验 + 包络软警告）；`EmsStrategy.vue` 弹窗一行替换并加 save() 校验闸。校验规则与后端 `PlanGenerator.java` 解析语义对齐（start≥end 静默 0 点、powerLimit≤0 按 0 功率、空窗口仅 STANDBY 尾点——前端提前拦截）；未知顶层键（含死字段 socRange）经 rest 原样保留。

**Tech Stack:** Vue 3.5 + Element Plus 2.9（全局注册）+ Vitest 2 + vue-tsc 2。

**Spec:** `docs/superpowers/specs/2026-08-08-ems-s1-design.md`

---

## Global Constraints

1. **纯前端，不触碰后端 API 契约**。无任何新接口、新依赖；`models.ts`、`dicts.ts` 不动（`EmsConstraint` 类型已存在）。
2. **表单仅覆盖 PEAK_VALLEY**：其余 4 类型（DEMAND/DR/SOC_CTRL/TIME）恒 JSON 文本域，仅 JSON 语法校验。
3. **socRange 表单不含、存量保留**：`serializePeakValley` 用 `{ ...rest, chargeWindows, dischargeWindows }` 合并回写未知顶层键，**不静默丢弃**。
4. **校验规则唯一来源 = `validatePeakValleyConfig`**（spec §3.2 规则 1-6）。报错文案逐字（spec 固定串，不得改动）：
   - 规则 1：`配置不是合法 JSON：{parse 错误信息}`（{parse 错误信息} 为 JSON.parse 异常 message）
   - 规则 2：`缺少 chargeWindows 或 dischargeWindows 数组`
   - 规则 3：`窗口 {i} 的开始/结束时间格式应为 HH:mm`（{i} 从 1 起）
   - 规则 4：`窗口 {i} 的结束时间必须晚于开始时间`
   - 规则 5：`窗口 {i} 的功率上限必须大于 0`
   - 规则 6：`请至少配置一个充电或放电窗口`
5. **软警告（不阻断）文案**：`充电窗口 {i} 功率上限 {x} kW 超过站点充电功率上限 {y} kW` / `放电窗口 {i} 功率上限 {x} kW 超过站点放电功率上限 {y} kW`。仅当 `envelope` 可用时计算；包络缺失/拉取失败静默跳过。
6. **模式进入规则（单一权威）**：config 空/未定义 → 结构化空窗口表；config 非空可解析 → 结构化；config 非空不可解析 → 强制 JSON 模式 + info 提示 `配置无法解析，已切换到 JSON 模式（结构化编辑会覆盖原配置）`，**不销毁用户数据**。
7. **其他固定串**：模式标签 `结构化编辑` / `JSON 模式`；按钮 `添加充电窗口` / `添加放电窗口`；空表 `暂无窗口`；JSON 合法 `JSON 语法正确`；格式化按钮 `格式化`。
8. **本会话 worktree 隔离的写文件规约**：Edit/Write 工具拒绝仓库根路径（"shared checkout"）。所有改动必须走「`cp` 仓库文件到临时目录 → Edit/Write 改临时副本 → `cp -f` 回仓库」；禁止直接 Edit/Write 仓库路径。`git` 命令从 `.worktrees\admin-pages` 运行，路径带 `../../` 前缀（见 §命令约定）。**禁止 `git add -A` / `git add .`** —— 会带上两台本地开发端口文件 `backend/energy-mqtt-broker/.../BrokerProperties.java` 与 `frontend/vite.config.ts`（它们已修改但属于机器本地配置，**不得提交**）；只 `git add` 任务明确列出的文件。禁止 bare `git stash`（共享 stash，会被并发会话弹出）。

## 命令约定

本会话 cwd 为空目录 `.worktrees\admin-pages`；仓库根即：

```bash
ROOT="D:/ProgramData/Codex-Data/Energy Storage IoT Platform"
TMP="$TMPDIR/ems-s1"          # Windows 下用 $TEMP；Git Bash 下 $TMPDIR=/tmp 亦可
mkdir -p "$TMP"
```

- 文件落地三步（以 `strategyConfig.ts` 为例）：
  ```bash
  cp "$ROOT/frontend/src/utils/strategyConfig.ts" "$TMP/strategyConfig.ts"   # 新文件可先 Write 到 $TMP 再 cp 回
  # 用 Edit/Write 工具改 "$TMP/strategyConfig.ts"
  cp -f "$TMP/strategyConfig.ts" "$ROOT/frontend/src/utils/strategyConfig.ts"
  ```
- 前端命令：`cd "$ROOT/frontend" && <cmd>`
- 提交（在 cwd 即 `.worktrees\admin-pages` 执行）：`git add ../../<repo-相对路径> && git commit -m "..."`
- 新建文件路径（不存在的目标直接 cp 即创建）：
  - `frontend/src/utils/strategyConfig.ts`
  - `frontend/src/utils/__tests__/strategyConfig.spec.ts`
  - `frontend/src/components/StrategyConfigEditor.vue`

---

### Task 1: `strategyConfig.ts` 纯逻辑模块 + 单测（TDD）

**Files:**
- Create: `frontend/src/utils/strategyConfig.ts`
- Test: `frontend/src/utils/__tests__/strategyConfig.spec.ts`

**Interfaces:**
- Produces（Task 2/3 依赖，签名不得改动）：
  - `interface TimeWindow { start: string; end: string; powerLimit: number }`
  - `interface PeakValleyConfig { chargeWindows: TimeWindow[]; dischargeWindows: TimeWindow[] }`
  - `parseJsonConfig(config: string): { ok: true; value: unknown } | { ok: false; error: string }`
  - `parsePeakValleyConfig(value: unknown): { ok: true; config: PeakValleyConfig; rest: Record<string, unknown> } | { ok: false; error: string }`
  - `validatePeakValleyConfig(config: string): string[]`（空数组 = 通过）
  - `serializePeakValley(config: PeakValleyConfig, rest: Record<string, unknown>): string`

- [ ] **Step 1: 写失败测试**

在 `frontend/src/utils/__tests__/strategyConfig.spec.ts` 写：

```ts
import { describe, expect, it } from 'vitest'
import {
  parseJsonConfig,
  parsePeakValleyConfig,
  serializePeakValley,
  validatePeakValleyConfig,
} from '@/utils/strategyConfig'

describe('strategyConfig', () => {
  const validConfig = `{
    "chargeWindows": [{"start": "02:00", "end": "06:00", "powerLimit": 100}],
    "dischargeWindows": [{"start": "18:00", "end": "22:00", "powerLimit": 80}],
    "socRange": {"min": 10, "max": 90}
  }`

  it('parseJsonConfig：合法 JSON', () => {
    expect(parseJsonConfig('{"a":1}').ok).toBe(true)
    expect(parseJsonConfig('"str"').ok).toBe(true)
  })

  it('parseJsonConfig：非法 JSON / 空串', () => {
    const bad = parseJsonConfig('{bad')
    expect(bad.ok).toBe(false)
    if (!bad.ok) expect(bad.error).toMatch(/配置不是合法 JSON/)
    expect(parseJsonConfig('').ok).toBe(false)
  })

  it('parsePeakValleyConfig：合法 → config + rest 保留未知键', () => {
    const r = parsePeakValleyConfig(JSON.parse(validConfig))
    expect(r.ok).toBe(true)
    if (r.ok) {
      expect(r.config.chargeWindows).toHaveLength(1)
      expect(r.config.chargeWindows[0]).toEqual({ start: '02:00', end: '06:00', powerLimit: 100 })
      expect(r.config.dischargeWindows).toHaveLength(1)
      expect(r.rest).toEqual({ socRange: { min: 10, max: 90 } })
    }
  })

  it('parsePeakValleyConfig：顶层非对象 / 缺数组', () => {
    expect(parsePeakValleyConfig('x').ok).toBe(false)
    expect(parsePeakValleyConfig([1, 2]).ok).toBe(false)
    expect(parsePeakValleyConfig({ chargeWindows: [] }).ok).toBe(false)
    expect(parsePeakValleyConfig({ chargeWindows: {}, dischargeWindows: [] }).ok).toBe(false)
  })

  it('parsePeakValleyConfig：坏时间格式', () => {
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '25:00', end: '06:00', powerLimit: 1 }], dischargeWindows: [] }).ok).toBe(false)
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '02:00', end: '06:60', powerLimit: 1 }], dischargeWindows: [] }).ok).toBe(false)
  })

  it('parsePeakValleyConfig：start >= end', () => {
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '06:00', end: '06:00', powerLimit: 1 }], dischargeWindows: [] }).ok).toBe(false)
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '08:00', end: '06:00', powerLimit: 1 }], dischargeWindows: [] }).ok).toBe(false)
  })

  it('parsePeakValleyConfig：powerLimit 缺失/0/负数', () => {
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '02:00', end: '06:00' }], dischargeWindows: [] }).ok).toBe(false)
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '02:00', end: '06:00', powerLimit: 0 }], dischargeWindows: [] }).ok).toBe(false)
    expect(parsePeakValleyConfig({ chargeWindows: [{ start: '02:00', end: '06:00', powerLimit: -5 }], dischargeWindows: [] }).ok).toBe(false)
  })

  it('parsePeakValleyConfig：至少一个窗口', () => {
    const r = parsePeakValleyConfig({ chargeWindows: [], dischargeWindows: [] })
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.error).toBe('请至少配置一个充电或放电窗口')
  })

  it('validatePeakValleyConfig：合法 → []', () => {
    expect(validatePeakValleyConfig(validConfig)).toEqual([])
  })

  it('validatePeakValleyConfig：语法错与结构化错各自返回首条', () => {
    expect(validatePeakValleyConfig('{bad')).toHaveLength(1)
    expect(validatePeakValleyConfig(JSON.stringify({
      chargeWindows: [{ start: '08:00', end: '06:00', powerLimit: 1 }],
      dischargeWindows: [],
    }))[0]).toMatch(/结束时间必须晚于开始时间/)
    expect(validatePeakValleyConfig('{"chargeWindows":[],"dischargeWindows":[]}')[0]).toMatch(/至少一个充电或放电窗口/)
  })

  it('serializePeakValley：rest 合并 + socRange 往返不丢 + 键序稳定', () => {
    const parsed = parsePeakValleyConfig(JSON.parse(validConfig))
    if (!parsed.ok) throw new Error('前置解析失败')
    const s = serializePeakValley(parsed.config, parsed.rest)
    const back = JSON.parse(s)
    expect(back.socRange).toEqual({ min: 10, max: 90 })
    expect(back.chargeWindows[0].powerLimit).toBe(100)
    expect(Object.keys(back)).toEqual(['socRange', 'chargeWindows', 'dischargeWindows'])
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "$ROOT/frontend" && npx vitest run src/utils/__tests__/strategyConfig.spec.ts`
Expected: FAIL —— `Cannot find module '@/utils/strategyConfig'` / `is not exported`。

- [ ] **Step 3: 实现模块**

在 `frontend/src/utils/strategyConfig.ts` 写：

```ts
/**
 * 策略配置 JSON 的解析 / 校验 / 序列化（纯函数，无 Vue 依赖）。
 * schema 契约与后端 PlanGenerator.java 解析规则对齐：
 *   - 仅 PEAK_VALLEY 有结构化 schema；窗口 {start:"HH:mm", end:"HH:mm", powerLimit}，end 排他（t.isBefore(end)）
 *   - start>=end 后端静默 0 点、powerLimit<=0 按 0 功率、空窗口仅 STANDBY 尾点（PlanGenerator.java:45,55,62）→ 前端提前拦截
 *   - socRange 等未知顶层键为死字段（生成器不读），序列化时经 rest 原样保留
 */

export interface TimeWindow {
  /** ISO LocalTime "HH:mm"，如 "02:00" */
  start: string
  /** ISO LocalTime "HH:mm"，排他（生成器 t.isBefore(end)） */
  end: string
  /** 窗口功率上限 kW，>0 */
  powerLimit: number
}

export interface PeakValleyConfig {
  chargeWindows: TimeWindow[]
  dischargeWindows: TimeWindow[]
}

export type ParseJsonResult =
  | { ok: true; value: unknown }
  | { ok: false; error: string }

export type ParsePeakValleyResult =
  | { ok: true; config: PeakValleyConfig; rest: Record<string, unknown> }
  | { ok: false; error: string }

/** "HH:mm"（0-23 时、0-59 分，零填充）。00:00 <= s < "24:00"，字典序即时序序。 */
const HHMM_RE = /^([01]\d|2[0-3]):[0-5]\d$/

/** 通用 JSON 语法校验（所有类型的 JSON 模式共用；空串/非法返回 error）。 */
export function parseJsonConfig(config: string): ParseJsonResult {
  try {
    return { ok: true, value: JSON.parse(config) }
  } catch (e) {
    return { ok: false, error: `配置不是合法 JSON：${e instanceof Error ? e.message : String(e)}` }
  }
}

/** 校验单个窗口（序号从 1 起）。返回首个错误文案；null = 通过。 */
function checkWindow(w: unknown, i: number): string | null {
  if (typeof w !== 'object' || w === null) return `窗口 ${i} 的开始/结束时间格式应为 HH:mm`
  const obj = w as Record<string, unknown>
  const { start, end, powerLimit } = obj
  if (typeof start !== 'string' || !HHMM_RE.test(start)) return `窗口 ${i} 的开始/结束时间格式应为 HH:mm`
  if (typeof end !== 'string' || !HHMM_RE.test(end)) return `窗口 ${i} 的开始/结束时间格式应为 HH:mm`
  if (start >= end) return `窗口 ${i} 的结束时间必须晚于开始时间`
  if (typeof powerLimit !== 'number' || !(powerLimit > 0)) return `窗口 ${i} 的功率上限必须大于 0`
  return null
}

/** PEAK_VALLEY 结构化校验：窗口数组、字段格式、start<end、powerLimit>0、至少一个窗口。 */
export function parsePeakValleyConfig(value: unknown): ParsePeakValleyResult {
  if (typeof value !== 'object' || value === null || Array.isArray(value)) {
    return { ok: false, error: '缺少 chargeWindows 或 dischargeWindows 数组' }
  }
  const obj = value as Record<string, unknown>
  if (!Array.isArray(obj.chargeWindows) || !Array.isArray(obj.dischargeWindows)) {
    return { ok: false, error: '缺少 chargeWindows 或 dischargeWindows 数组' }
  }
  const chargeWindows: TimeWindow[] = []
  for (let i = 0; i < obj.chargeWindows.length; i++) {
    const err = checkWindow(obj.chargeWindows[i], i + 1)
    if (err) return { ok: false, error: err }
    chargeWindows.push(obj.chargeWindows[i] as TimeWindow)
  }
  const dischargeWindows: TimeWindow[] = []
  for (let i = 0; i < obj.dischargeWindows.length; i++) {
    const err = checkWindow(obj.dischargeWindows[i], i + 1)
    if (err) return { ok: false, error: err }
    dischargeWindows.push(obj.dischargeWindows[i] as TimeWindow)
  }
  if (chargeWindows.length === 0 && dischargeWindows.length === 0) {
    return { ok: false, error: '请至少配置一个充电或放电窗口' }
  }
  const rest: Record<string, unknown> = {}
  for (const [k, v] of Object.entries(obj)) {
    if (k !== 'chargeWindows' && k !== 'dischargeWindows') rest[k] = v
  }
  return { ok: true, config: { chargeWindows, dischargeWindows }, rest }
}

/** 便捷入口：config 字符串 → 问题列表（空数组 = 通过）。save() 闸与组件内联提示共用。 */
export function validatePeakValleyConfig(config: string): string[] {
  const parsed = parseJsonConfig(config)
  if (!parsed.ok) return [parsed.error]
  const structured = parsePeakValleyConfig(parsed.value)
  if (!structured.ok) return [structured.error]
  return []
}

/** 序列化：{ ...rest, chargeWindows, dischargeWindows }，未知顶层键（含 socRange）原样保留。 */
export function serializePeakValley(config: PeakValleyConfig, rest: Record<string, unknown>): string {
  return JSON.stringify(
    { ...rest, chargeWindows: config.chargeWindows, dischargeWindows: config.dischargeWindows },
    null,
    2,
  )
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "$ROOT/frontend" && npx vitest run src/utils/__tests__/strategyConfig.spec.ts`
Expected: PASS，全部用例。

- [ ] **Step 5: 落地 + 提交**

按 §命令约定落地两个文件（新建：Write 到 `$TMP/strategyConfig.ts` 与 `$TMP/strategyConfig.spec.ts`，再 `cp -f` 到 `frontend/src/utils/` 与 `frontend/src/utils/__tests__/`），然后：

```bash
git add ../../frontend/src/utils/strategyConfig.ts ../../frontend/src/utils/__tests__/strategyConfig.spec.ts
git commit -m "feat(ems): strategyConfig 纯逻辑模块（PEAK_VALLEY config 解析/校验/序列化）+ 单测"
```

---

### Task 2: `StrategyConfigEditor.vue` 结构化表单子组件

**Files:**
- Create: `frontend/src/components/StrategyConfigEditor.vue`

**Interfaces:**
- Consumes: Task 1 全部导出（`parseJsonConfig` / `parsePeakValleyConfig` / `serializePeakValley` / `validatePeakValleyConfig` / `TimeWindow` / `PeakValleyConfig`）+ `EmsConstraint` 类型（`@/types/models`，已有）。
- Produces（Task 3 依赖）：`<StrategyConfigEditor v-model="config" :strategy-type="..." :envelope="...">` —— `modelValue?: string`（默认 `''`）、`strategyType?: string`（默认 `''`）、`envelope?: EmsConstraint | null`（默认 `null`）；emit `update:modelValue(value: string)`。

- [ ] **Step 1: 实现组件**

在 `frontend/src/components/StrategyConfigEditor.vue` 写（完整文件）：

```vue
<script setup lang="ts">
import { computed, ref, watch } from 'vue'
import { ElMessage } from 'element-plus'
import type { EmsConstraint } from '@/types/models'
import {
  parseJsonConfig,
  parsePeakValleyConfig,
  serializePeakValley,
  validatePeakValleyConfig,
  type PeakValleyConfig,
} from '@/utils/strategyConfig'

const props = withDefaults(defineProps<{
  modelValue?: string
  strategyType?: string
  envelope?: EmsConstraint | null
}>(), {
  modelValue: '',
  strategyType: '',
  envelope: null,
})

const emit = defineEmits<{ (e: 'update:modelValue', value: string): void }>()

type Mode = 'form' | 'json'
const mode = ref<Mode>('form')
/** 结构化模式的可编辑窗口表（仅 PEAK_VALLEY 使用） */
const form = ref<PeakValleyConfig>({ chargeWindows: [], dischargeWindows: [] })
/** 上次成功解析保留的未知顶层键（socRange 等），序列化时合并回写 */
const rest = ref<Record<string, unknown>>({})
/** 阻断性校验问题（内联红显） */
const issues = ref<string[]>([])
/** JSON 模式实时语法错误（'' = 通过） */
const jsonError = ref('')
/** 非空不可解析 → 强制 JSON 模式的 info 提示开关 */
const forceJson = ref(false)

const isPeakValley = computed(() => props.strategyType === 'PEAK_VALLEY')

/** 包络软警告（不阻断）：窗口功率超站点安全约束上限 */
const warnings = computed(() => {
  const list: string[] = []
  if (!isPeakValley.value || !props.envelope) return list
  // 先取局部 const：属性收窄不跨 forEach 闭包（strict 下 "possibly null"），局部 const 收窄可跨闭包
  const chargeMax = props.envelope.chargePowerMax
  if (chargeMax != null) {
    form.value.chargeWindows.forEach((w, i) => {
      if (w.powerLimit > chargeMax) {
        list.push(`充电窗口 ${i + 1} 功率上限 ${w.powerLimit} kW 超过站点充电功率上限 ${chargeMax} kW`)
      }
    })
  }
  const dischargeMax = props.envelope.dischargePowerMax
  if (dischargeMax != null) {
    form.value.dischargeWindows.forEach((w, i) => {
      if (w.powerLimit > dischargeMax) {
        list.push(`放电窗口 ${i + 1} 功率上限 ${w.powerLimit} kW 超过站点放电功率上限 ${dischargeMax} kW`)
      }
    })
  }
  return list
})

/** 最近一次 emit 回写的 config；外部 modelValue === 它 = 自回写，跳过重解析防循环 */
let lastEmitted: string | null = null

function jsonErrorOf(config: string): string {
  const r = parseJsonConfig(config)
  return r.ok ? '' : r.error
}

/** 模式进入规则（spec §3.3 单一权威）：空→结构化空表；非空可解析→结构化；非空不可解析→强制 JSON。 */
function initFromConfig() {
  const raw = props.modelValue ?? ''
  if (!isPeakValley.value) {
    mode.value = 'json'
    jsonError.value = jsonErrorOf(raw)
    forceJson.value = false
    return
  }
  if (raw.trim() === '') {
    mode.value = 'form'
    form.value = { chargeWindows: [], dischargeWindows: [] }
    rest.value = {}
    issues.value = []
    forceJson.value = false
    return
  }
  const parsed = parseJsonConfig(raw)
  if (!parsed.ok) {
    mode.value = 'json'
    jsonError.value = parsed.error
    forceJson.value = true
    return
  }
  const structured = parsePeakValleyConfig(parsed.value)
  if (!structured.ok) {
    mode.value = 'json'
    jsonError.value = structured.error
    forceJson.value = true
    return
  }
  mode.value = 'form'
  form.value = structured.config
  rest.value = structured.rest
  issues.value = []
  forceJson.value = false
}

function emitConfig() {
  const s = serializePeakValley(form.value, rest.value)
  lastEmitted = s
  emit('update:modelValue', s)
}

function switchMode(m: Mode) {
  if (m === mode.value) return
  if (m === 'json') {
    if (isPeakValley.value) emitConfig() // 结构化 → JSON：当前表单序列化入文本域
    jsonError.value = jsonErrorOf(props.modelValue)
    mode.value = 'json'
  } else {
    // JSON → 结构化：重新 parse，失败保持 JSON 模式
    const parsed = parseJsonConfig(props.modelValue)
    if (!parsed.ok) {
      ElMessage.error(parsed.error)
      return
    }
    const structured = parsePeakValleyConfig(parsed.value)
    if (!structured.ok) {
      ElMessage.error(structured.error)
      return
    }
    form.value = structured.config
    rest.value = structured.rest
    mode.value = 'form'
  }
}

function onJsonInput(v: string) {
  lastEmitted = v
  emit('update:modelValue', v)
  jsonError.value = jsonErrorOf(v)
}

function formatJson() {
  const r = parseJsonConfig(props.modelValue)
  if (!r.ok) {
    ElMessage.error(r.error)
    return
  }
  const s = JSON.stringify(r.value, null, 2)
  lastEmitted = s
  emit('update:modelValue', s)
  jsonError.value = ''
}

function addWindow(kind: 'chargeWindows' | 'dischargeWindows') {
  form.value[kind].push({ start: '00:00', end: '01:00', powerLimit: 100 })
}

function removeWindow(kind: 'chargeWindows' | 'dischargeWindows', index: number) {
  form.value[kind].splice(index, 1)
}

const windowGroups = [
  { key: 'chargeWindows' as const, label: '充电窗口', addLabel: '添加充电窗口' },
  { key: 'dischargeWindows' as const, label: '放电窗口', addLabel: '添加放电窗口' },
]

watch(
  () => props.modelValue,
  (val) => {
    if (val === lastEmitted) return // 自 emit 回写，避免循环
    lastEmitted = null
    initFromConfig()
  },
)

watch(
  () => props.strategyType,
  () => initFromConfig(),
)

watch(
  form,
  () => {
    issues.value = validatePeakValleyConfig(serializePeakValley(form.value, rest.value))
    emitConfig()
  },
  { deep: true },
)

initFromConfig()
</script>

<template>
  <div class="strategy-config-editor">
    <div v-if="isPeakValley" class="mode-bar">
      <el-radio-group :model-value="mode" size="small" @update:model-value="switchMode($event as Mode)">
        <el-radio-button value="form">结构化编辑</el-radio-button>
        <el-radio-button value="json">JSON 模式</el-radio-button>
      </el-radio-group>
    </div>

    <!-- 结构化模式（仅 PEAK_VALLEY 可达） -->
    <template v-if="isPeakValley && mode === 'form'">
      <div v-for="group in windowGroups" :key="group.key" class="window-group">
        <div class="group-head">
          <span class="group-label">{{ group.label }}</span>
          <el-button link type="primary" size="small" @click="addWindow(group.key)">{{ group.addLabel }}</el-button>
        </div>
        <div v-if="form[group.key].length === 0" class="group-empty">暂无窗口</div>
        <div v-for="(w, i) in form[group.key]" :key="i" class="window-row">
          <el-time-picker v-model="w.start" format="HH:mm" value-format="HH:mm" placeholder="开始" :clearable="false" style="width: 110px" />
          <span class="sep">至</span>
          <el-time-picker v-model="w.end" format="HH:mm" value-format="HH:mm" placeholder="结束" :clearable="false" style="width: 110px" />
          <el-input-number v-model="w.powerLimit" :min="0.1" :precision="1" :step="1" style="width: 130px" />
          <span class="unit">kW</span>
          <el-button link type="danger" size="small" @click="removeWindow(group.key, i)">删除</el-button>
        </div>
      </div>
      <el-alert
        v-if="issues.length"
        type="error"
        :closable="false"
        class="block-alert"
        :title="issues.join('；')"
      />
      <el-alert
        v-if="warnings.length"
        type="warning"
        :closable="false"
        class="warn-alert"
        :title="warnings.join('；')"
      />
    </template>

    <!-- JSON 模式（非 PEAK_VALLEY 恒为 JSON；PEAK_VALLEY 切出/不可解析时） -->
    <template v-else-if="mode === 'json'">
      <el-alert
        v-if="isPeakValley && forceJson"
        type="info"
        :closable="false"
        class="mode-alert"
        title="配置无法解析，已切换到 JSON 模式（结构化编辑会覆盖原配置）"
      />
      <el-input
        :model-value="props.modelValue"
        type="textarea"
        :rows="6"
        placeholder='{"chargeWindows":[{"start":"02:00","end":"06:00","powerLimit":100}]}'
        @update:model-value="onJsonInput($event as string)"
      />
      <div class="json-toolbar">
        <span class="json-state" :class="jsonError ? 'err' : 'ok'">
          {{ jsonError || 'JSON 语法正确' }}
        </span>
        <el-button link type="primary" size="small" @click="formatJson">格式化</el-button>
      </div>
    </template>
  </div>
</template>

<style scoped>
.mode-bar {
  margin-bottom: 8px;
}
.mode-alert {
  margin-bottom: 8px;
}
.window-group {
  margin-bottom: 10px;
}
.group-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 6px;
}
.group-label {
  font-weight: 600;
  font-size: 13px;
}
.group-empty {
  color: var(--el-text-color-secondary);
  font-size: 12px;
  padding: 6px 0;
}
.window-row {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 6px;
}
.sep {
  color: var(--el-text-color-secondary);
}
.unit {
  color: var(--el-text-color-secondary);
  font-size: 12px;
}
.block-alert {
  margin-top: 8px;
}
.warn-alert {
  margin-top: 8px;
}
.json-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 6px;
  font-size: 12px;
}
.json-state.ok {
  color: var(--el-color-success);
}
.json-state.err {
  color: var(--el-color-danger);
}
</style>
```

- [ ] **Step 2: 类型检查**

Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0，无新增错误。

- [ ] **Step 3: 落地 + 提交**

按 §命令约定落地 `StrategyConfigEditor.vue`（Write 到 `$TMP/StrategyConfigEditor.vue` → `cp -f` 到 `frontend/src/components/`），然后：

```bash
git add ../../frontend/src/components/StrategyConfigEditor.vue
git commit -m "feat(ems): StrategyConfigEditor 结构化配置表单子组件（窗口表 + 模式切换 + 内联校验 + 包络软警告）"
```

---

### Task 3: `EmsStrategy.vue` 集成（替换 textarea + 包络拉取 + save 校验闸）

**Files:**
- Modify: `frontend/src/views/EmsStrategy.vue`

**Interfaces:**
- Consumes: `StrategyConfigEditor`（Task 2）、`parseJsonConfig` / `validatePeakValleyConfig`（Task 1）、`EmsConstraint` 类型、`emsApi.constraintGet(stationId: string): Promise<EmsConstraint>`（已有）。

- [ ] **Step 1: 脚本区 import 调整**

`EmsStrategy.vue:5`：

```ts
import type { EmsStrategy } from '@/types/models'
```

改为：

```ts
import type { EmsConstraint, EmsStrategy } from '@/types/models'
```

在 `import { isStrategyGeneratable } from '@/utils/dicts'`（第 6 行）之后新增：

```ts
import StrategyConfigEditor from '@/components/StrategyConfigEditor.vue'
import { parseJsonConfig, validatePeakValleyConfig } from '@/utils/strategyConfig'
```

- [ ] **Step 2: 新增 envelope ref + 包络拉取**

在 `const isEdit = ref(false)`（第 15 行）之后新增：

```ts
/** 站点安全包络（供配置表单软警告）；缺失/拉取失败静默置 null */
const envelope = ref<EmsConstraint | null>(null)
```

`openCreate` / `openEdit`（第 54-55 行）改为：

```ts
function openCreate() {
  editing.value = {}
  isEdit.value = false
  dialogVisible.value = true
  void loadEnvelope()
}
function openEdit(row: EmsStrategy) {
  editing.value = { ...row }
  isEdit.value = true
  dialogVisible.value = true
  void loadEnvelope()
}
async function loadEnvelope() {
  envelope.value = null
  if (!editing.value.stationId) return
  try {
    envelope.value = await emsApi.constraintGet(editing.value.stationId)
  } catch {
    envelope.value = null // 包络缺失/未配置：软警告静默跳过
  }
}
```

- [ ] **Step 3: save() 前置校验闸**

`save()`（第 57-58 行）在 S2 生成类型软警告之前插入校验闸（`try` 之前、`ElMessage.warning` 之前）：

```ts
async function save() {
  const raw = editing.value.config ?? ''
  if (editing.value.strategyType === 'PEAK_VALLEY') {
    const issues = validatePeakValleyConfig(raw)
    if (issues.length) {
      ElMessage.error(issues[0])
      return
    }
  } else {
    const r = parseJsonConfig(raw)
    if (!r.ok) {
      ElMessage.error(r.error)
      return
    }
  }
  if (editing.value.strategyType && !isStrategyGeneratable(editing.value.strategyType)) {
    ElMessage.warning('当前仅峰谷套利可生成计划，其余类型可保存但不可生成')
  }
  try {
    ...
```

- [ ] **Step 4: 模板替换配置 JSON 表单项**

`EmsStrategy.vue:194-196`：

```vue
<el-form-item label="配置 JSON">
  <el-input v-model="editing.config" type="textarea" :rows="5" placeholder='{"chargeWindows":[{"start":"02:00","end":"06:00","powerLimit":100}]}' />
</el-form-item>
```

改为：

```vue
<el-form-item label="配置 JSON">
  <StrategyConfigEditor v-model="editing.config" :strategy-type="editing.strategyType" :envelope="envelope" />
</el-form-item>
```

- [ ] **Step 5: 类型检查 + 全量单测**

Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0。
Run: `cd "$ROOT/frontend" && npm test`
Expected: 全绿（含 Task 1 新增用例）。

- [ ] **Step 6: 落地 + 提交**

按 §命令约定落地 `EmsStrategy.vue`，然后：

```bash
git add ../../frontend/src/views/EmsStrategy.vue
git commit -m "feat(ems): 策略页接入结构化配置表单 + 保存前校验闸 + 包络软警告"
```

---

### Task 4: 全量回归 + 提交复核

**Files:** 无新改动；仅验证。

- [ ] **Step 1: 全量类型检查**

Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0。

- [ ] **Step 2: 全量单测**

Run: `cd "$ROOT/frontend" && npm test`
Expected: 全绿（基线 59 断言 + Task 1 新增 ~14 条）。

- [ ] **Step 3: 提交范围复核**

Run: `git status --short`
Expected: 恰好包含本迭代 4 个文件（新建 `strategyConfig.ts`、`strategyConfig.spec.ts`、`StrategyConfigEditor.vue`；修改 `EmsStrategy.vue`）的已提交记录，且工作区只剩两台本地端口文件（`BrokerProperties.java`、`vite.config.ts`）为未提交 M。确认 3 个提交已落：`feat(ems): strategyConfig …`、`feat(ems): StrategyConfigEditor …`、`feat(ems): 策略页接入 …`。

- [ ] **Step 4: 手工冒烟（spec §3.5，需本地全栈已启动）**

   1. 新建 PEAK_VALLEY 策略：结构化模式添加充/放窗口（时间选择器选起止、功率输入 100/80）→ 保存成功 → 编辑回读 JSON 键为 `chargeWindows`/`dischargeWindows`。
   2. 窗口 start≥end（或 powerLimit=0）→ 内联红显 + 保存被拦（`ElMessage.error` 首条）。
   3. 切换 JSON 模式 → 文本域内容 = 序列化结果；改坏语法 → 红错「配置不是合法 JSON：…」，保存被拦；点「格式化」→ 排版恢复。
   4. 存量 config 含 `socRange` → 结构化编辑后保存，回读 config 中 `socRange` 仍在。
   5. 站点已配约束：窗口 powerLimit 超 `chargePowerMax`/`dischargePowerMax` → 软警告出现，但不阻断保存。
   6. DEMAND 类型策略 → 仅 JSON 文本域；语法错保存被拦。
   7. 打开一条 config 无法解析的历史策略 → 强制 JSON 模式 + info 提示，原文本不被覆盖。

   若本地服务未启动，标记「待运行时冒烟」并在回归报告中明示，不静默跳过。

- [ ] **Step 5: 无遗留改动则无新提交**（若冒烟发现修复项，按落地规约补提交并注明）。

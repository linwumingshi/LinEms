# EMS P1 三件套实施计划（电站名称化 / 计划页内联生成 / 复制策略）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 纯前端三件套——策略/计划页电站名称化（下拉+列表+副题显示名称）、计划页内联「生成计划」弹窗（日期+电站+按站过滤的策略下拉，调 `planGenerate`）、策略页「复制」行操作（预填新增弹窗名称加「副本」）。

**Architecture:** 新增 `stationApi`（`GET /api/station/page`）+ `Station` 类型 + `utils/stationDict.ts`（模块级缓存加载 + 名称解析纯函数）作为两页共享的数据源；`EmsStrategy.vue` 加电站下拉/列表电站列/复制/包络 watch；`EmsPlan.vue` 加名称映射、副题与列表名称化、内联生成弹窗。全部后端已就绪，零后端改动。

**Tech Stack:** Vue 3.5 + Element Plus 2.9（全局注册）+ axios（http.ts 封装）+ Vitest 2 + vue-tsc 2。

**Spec:** `docs/superpowers/specs/2026-08-08-ems-p1-design.md`

---

## Global Constraints

1. **纯前端，不触碰后端 API 契约**。无任何新接口、新依赖、无后端改动。
2. **`pageNum` 契约**：`stationApi.stationPage` 必须传 `{pageNum, pageSize}`，**严禁用 EMS 的 `pageNo`** —— 后端 `StationQuery` 只认 `pageNum`，传 `pageNo` 会被忽略、回落默认 pageSize=20（少于 100 的电站会被截断）。EMS 模块内部照旧用 `pageNo`，两套不要混。
3. **名称回退规则**：任何 id 在 stations / strategies 列表查不到 → 显示裸 id；`undefined/null` → 空串。**绝不空白**。
4. **文案逐字**（spec §4，不得改动）：复制后缀「副本」（`${row.strategyName} 副本`）、策略下拉动态 placeholder（已选电站 →「自动选择（启用中优先级最高）」，未选 →「请先选择电站」）、成功 toast「计划已生成」、电站加载失败 toast「电站列表加载失败，请稍后重试」。
5. **保存只提交 DTO 字段**：`save()` 已显式构造 payload（S1 修复 `4ba4052`），复制走同一 `save()`，不得把整行 row 直发后端。
6. **本会话 worktree 隔离的写文件规约**：Edit/Write 工具拒绝仓库根路径（"shared checkout"）。所有改动必须走「`cp` 仓库文件到临时目录 → Edit/Write 改临时副本 → `cp -f` 回仓库」；禁止直接 Edit/Write 仓库路径。`git` 命令从 `.worktrees\admin-pages` 运行，路径带 `../../` 前缀（见 §命令约定）。**禁止 `git add -A` / `git add .`** —— 会带上两台本地开发端口文件 `backend/energy-mqtt-broker/.../BrokerProperties.java` 与 `frontend/vite.config.ts`（它们已修改但属于机器本地配置，**不得提交**）；只 `git add` 任务明确列出的文件。禁止 bare `git stash`（共享 stash，会被并发会话弹出）。

## 命令约定

本会话 cwd 为空目录 `.worktrees\admin-pages`；仓库根即：

```bash
ROOT="D:/ProgramData/Codex-Data/Energy Storage IoT Platform"
TMP="$TMPDIR/ems-p1"          # Windows 下用 $TEMP；Git Bash 下 $TMPDIR=/tmp 亦可
mkdir -p "$TMP"
```

- 文件落地三步（以 `station.ts` 为例）：
  ```bash
  cp "$ROOT/frontend/src/api/station.ts" "$TMP/station.ts"   # 新建文件无需 cp；直接 Edit/Write 临时副本
  # 用 Edit 工具改 "$TMP/station.ts"
  cp -f "$TMP/station.ts" "$ROOT/frontend/src/api/station.ts"
  ```
- 前端命令：`cd "$ROOT/frontend" && <cmd>`（PowerShell 用 `Set-Location` + `& node_modules\.bin\...`）
- 提交（在 cwd 即 `.worktrees\admin-pages` 执行）：`git add ../../<repo-相对路径> && git commit -m "..."`

---

### Task 1: 基础设施——`Station` 类型 + `stationApi` + `stationDict`（TDD）

**Files:**
- Modify: `frontend/src/types/models.ts`（`EmsElectricityPrice` 段后、`// ---------------- 产品 Product ----------------` 前插入）
- Create: `frontend/src/api/station.ts`
- Create: `frontend/src/utils/stationDict.ts`
- Test: `frontend/src/utils/__tests__/stationDict.spec.ts`

**Interfaces:**
- Produces: `stationApi.stationPage(params: Record<string, unknown>): Promise<PageResult<Station>>`；`loadStations(force = false): Promise<Station[]>`；`_resetStationCache(): void`；`stationName(id: string | number | null | undefined, stations: Station[]): string`；`Station` 类型。Task 2 / Task 3 依赖。

- [ ] **Step 1: 写失败测试**

创建 `frontend/src/utils/__tests__/stationDict.spec.ts`（在 `$TMP/stationDict.spec.ts`）：

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { stationApi } from '@/api/station'
import { loadStations, stationName, _resetStationCache } from '@/utils/stationDict'

vi.mock('@/api/station', () => ({
  stationApi: { stationPage: vi.fn() },
}))

const mockStationPage = vi.mocked(stationApi.stationPage)

const stations = [
  { stationId: '1', stationName: '深圳一号站', status: 1 },
  { stationId: '2', stationName: '东莞储能站', status: 1 },
]

const pageResult = { records: stations, total: 2, pages: 1, current: 1, size: 100 }

beforeEach(() => {
  _resetStationCache()
  mockStationPage.mockReset()
  mockStationPage.mockResolvedValue(pageResult as never)
})

describe('stationDict', () => {
  it('loadStations 首次拉取后二次调用不重复请求（缓存）', async () => {
    const first = await loadStations()
    expect(mockStationPage).toHaveBeenCalledTimes(1)
    expect(mockStationPage).toHaveBeenCalledWith({ pageNum: 1, pageSize: 100 })
    const second = await loadStations()
    expect(mockStationPage).toHaveBeenCalledTimes(1)
    expect(second).toBe(first)
  })

  it('loadStations(force=true) 强制重拉', async () => {
    await loadStations()
    await loadStations(true)
    expect(mockStationPage).toHaveBeenCalledTimes(2)
  })

  it('_resetStationCache 后重新拉取', async () => {
    await loadStations()
    _resetStationCache()
    await loadStations()
    expect(mockStationPage).toHaveBeenCalledTimes(2)
  })

  it('stationName：已知回名称 / 未知回原 id / 空回空串', () => {
    expect(stationName('1', stations)).toBe('深圳一号站')
    expect(stationName(2, stations)).toBe('东莞储能站')
    expect(stationName('999', stations)).toBe('999')
    expect(stationName(undefined, stations)).toBe('')
    expect(stationName(null, stations)).toBe('')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "$ROOT/frontend" && npx vitest run src/utils/__tests__/stationDict.spec.ts`
Expected: FAIL —— `Cannot find module '@/api/station'` / `Cannot find module '@/utils/stationDict'`。

- [ ] **Step 3: 写实现**

**`types/models.ts`** 在 `EmsElectricityPrice` 接口结束（`}` + 空行）之后、`// ---------------- 产品 Product ----------------` 之前插入：

```ts
// ---------------- 电站 Station ----------------

/** 电站资产（iot_station；stationId 为 Long，序列化为字符串，同雪花约定） */
export interface Station {
  stationId: string
  enterpriseId?: string | null
  stationCode?: string | null
  stationName: string
  address?: string | null
  gridType?: string | null
  status?: number
}
```

**`api/station.ts`** 创建：

```ts
import http from './http'
import type { PageResult, Station } from '@/types/models'

/** 电站资产 API（网关路由 /api/station/** → energy-station，StripPrefix=1） */
export const stationApi = {
  /** GET /api/station/page 分页查询（后端 StationQuery 用 pageNum，勿与 EMS 的 pageNo 混淆） */
  stationPage(params: Record<string, unknown>): Promise<PageResult<Station>> {
    return http.get('/api/station/page', { params })
  },
}
```

**`utils/stationDict.ts`** 创建：

```ts
/**
 * 电站列表加载与名称解析（纯逻辑，Vue 无关）。
 * 模块级缓存避免策略页/计划页重复请求；名称查不到回退裸 id，绝不空白。
 * 后端契约：GET /api/station/page 分页参数是 pageNum（StationQuery），非 EMS 的 pageNo。
 */
import { stationApi } from '@/api/station'
import type { Station } from '@/types/models'

let cached: Station[] | null = null

/** 电站列表（首次拉取后缓存；force=true 强制重拉）。失败向上抛，由调用方决定提示策略。 */
export async function loadStations(force = false): Promise<Station[]> {
  if (cached && !force) return cached
  const page = await stationApi.stationPage({ pageNum: 1, pageSize: 100 })
  cached = page.records
  return cached
}

/** 测试用：清空缓存 */
export function _resetStationCache(): void {
  cached = null
}

/** 名称解析：已知 id → stationName；未知 → 回退原 id；空 → 空串。 */
export function stationName(id: string | number | null | undefined, stations: Station[]): string {
  const key = String(id ?? '')
  if (!key) return ''
  return stations.find((s) => String(s.stationId) === key)?.stationName ?? key
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "$ROOT/frontend" && npx vitest run src/utils/__tests__/stationDict.spec.ts`
Expected: PASS，4 用例全绿（缓存复用、force、reset、stationName 回退）。

- [ ] **Step 5: 落地 + 类型检查 + 提交**

按 §命令约定落地三个新文件（models.ts 用 cp 到临时副本改完再 cp 回；`station.ts` / `stationDict.ts` / `stationDict.spec.ts` 直接 Edit/Write 临时路径后 cp 回仓库）。
Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0。

```bash
git add ../../frontend/src/types/models.ts ../../frontend/src/api/station.ts ../../frontend/src/utils/stationDict.ts ../../frontend/src/utils/__tests__/stationDict.spec.ts
git commit -m "feat(ems): Station 类型 + stationApi + stationDict 缓存字典（电站名称化基础设施）+ 单测"
```

---

### Task 2: `EmsStrategy.vue` 电站下拉 / 电站列 / 复制 / 包络 watch

**Files:**
- Modify: `frontend/src/views/EmsStrategy.vue`

**Interfaces:**
- Consumes: `loadStations` / `stationName` / `Station` 类型（Task 1）。
- 语义：电站字段由裸 ID `el-input` 改为 `el-select`（label=`stationName`、value=`stationId`）；列表加「电站」列；行操作加「复制」；`watch(stationId)` 重载包络。

- [ ] **Step 1: 脚本区 import**

`frontend/src/views/EmsStrategy.vue:1` 由

```ts
import { computed, onMounted, ref } from 'vue'
```

改为

```ts
import { computed, onMounted, ref, watch } from 'vue'
```

`:4` 由

```ts
import type { EmsConstraint, EmsStrategy } from '@/types/models'
```

改为

```ts
import type { EmsConstraint, EmsStrategy, Station } from '@/types/models'
```

在 `:5`（`import { isStrategyGeneratable } from '@/utils/dicts'`）之后新增：

```ts
import { loadStations, stationName } from '@/utils/stationDict'
```

- [ ] **Step 2: 新增电站数据源状态**

在 `:19`（`const envelope = ref<EmsConstraint | null>(null)`）之后新增：

```ts
/** 电站下拉/列表名称数据源（loadStations 模块级缓存；失败回退裸 id） */
const stations = ref<Station[]>([])
```

- [ ] **Step 3: 电站列表加载 + 挂在 onMounted**

在 `load()` 函数（`:47-56`）之后新增：

```ts
async function loadStationOptions() {
  try {
    stations.value = await loadStations()
  } catch {
    ElMessage.error('电站列表加载失败，请稍后重试')
  }
}
```

`:165` `onMounted(load)` 改为：

```ts
onMounted(() => {
  void load()
  void loadStationOptions()
})
```

- [ ] **Step 4: 复制行操作**

在 `openEdit()`（`:64-69`）之后新增：

```ts
function copyStrategy(row: EmsStrategy) {
  editing.value = {
    stationId: row.stationId,
    strategyName: `${row.strategyName} 副本`,
    strategyType: row.strategyType,
    config: row.config,
    priority: row.priority,
  }
  isEdit.value = false
  dialogVisible.value = true
  void loadEnvelope()
}
```

- [ ] **Step 5: 包络 watch（下拉切换电站即重载）**

在 `loadEnvelope()`（`:70-81`）之后新增：

```ts
/** 下拉切换电站即重载包络（S1 竞态守卫已处理异步过期） */
watch(() => editing.value.stationId, () => void loadEnvelope())
```

- [ ] **Step 6: 模板列表加「电站」列**

在类型列 `</el-table-column>`（`:187`）之后新增：

```vue
<el-table-column label="电站" min-width="120" show-overflow-tooltip>
  <template #default="{ row }">{{ stationName(row.stationId, stations) }}</template>
</el-table-column>
```

- [ ] **Step 7: 模板行操作加「复制」**

`:203`（`<el-button link type="primary" @click="openEdit(row)">编辑</el-button>`）之后新增：

```vue
<el-button link type="primary" @click="copyStrategy(row)">复制</el-button>
```

`:201` 操作列宽度 `width="280"` 改为 `width="340"`（容纳第 5 个按钮）。

- [ ] **Step 8: 模板电站字段 el-input → el-select**

`:241-243`：

```vue
<el-form-item label="电站 ID">
  <el-input v-model="editing.stationId" placeholder="电站 ID" clearable style="width: 200px" />
</el-form-item>
```

改为：

```vue
<el-form-item label="电站">
  <el-select v-model="editing.stationId" placeholder="请选择电站" filterable clearable style="width: 100%">
    <el-option v-for="s in stations" :key="s.stationId" :label="s.stationName" :value="s.stationId" />
  </el-select>
</el-form-item>
```

- [ ] **Step 9: 类型检查 + 落地 + 提交**

Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0，无新增错误。

按 §命令约定落地 `EmsStrategy.vue`：

```bash
git add ../../frontend/src/views/EmsStrategy.vue
git commit -m "feat(ems): 策略页电站下拉+列表电站列+复制行操作+包络 watch"
```

---

### Task 3: `EmsPlan.vue` 名称化 + 内联「生成计划」弹窗

**Files:**
- Modify: `frontend/src/views/EmsPlan.vue`

**Interfaces:**
- Consumes: `loadStations` / `stationName`（Task 1）；`emsApi.strategyPage({pageNo, pageSize, stationId, status})`；`emsApi.planGenerate({stationId, strategyId?, planDate})`（既有）。
- 语义：副题与列表显示站/策略名；页头「生成计划」→ 弹窗（日期 + 电站 + 按站过滤的启用策略下拉，可空自动选择）。

- [ ] **Step 1: 脚本区 import 与状态**

`frontend/src/views/EmsPlan.vue:5` 由

```ts
import type { EmsElectricityPrice, EmsPlan, EmsPlanPoint } from '@/types/models'
```

改为

```ts
import type { EmsElectricityPrice, EmsPlan, EmsPlanPoint, EmsStrategy, Station } from '@/types/models'
```

之后新增：

```ts
import { loadStations, stationName } from '@/utils/stationDict'
```

在 `:15`（`const { chart, render } = useEChart(chartEl)`）之后新增：

```ts
/** 名称化：电站/策略 id → 名称映射（查不到回退裸 id，见 Global Constraints 3） */
const stations = ref<Station[]>([])
const strategies = ref<EmsStrategy[]>([])
function strategyLabel(id: string | undefined): string {
  return strategies.value.find((s) => String(s.strategyId) === String(id))?.strategyName ?? String(id ?? '')
}
```

- [ ] **Step 2: 名称映射加载 + 挂在 onMounted**

在 `load()`（`:62-73`）之后新增：

```ts
async function loadMaps() {
  try {
    const [stationList, strategyPage] = await Promise.all([
      loadStations(),
      emsApi.strategyPage({ pageNo: 1, pageSize: 100 }),
    ])
    stations.value = stationList
    strategies.value = strategyPage.records
  } catch {
    // 名称回退裸 id，不阻断页面
  }
}
```

`:204` `onMounted(load)` 改为：

```ts
onMounted(() => {
  void load()
  void loadMaps()
})
```

- [ ] **Step 3: 生成弹窗状态与处理函数**

在 `dispatch()`（`:190-198`）之后新增：

```ts
// ---- 内联「生成计划」弹窗 ----
const genDialogVisible = ref(false)
const generating = ref(false)
const genForm = ref<{ planDate: string; stationId?: string; strategyId?: string }>({ planDate: todayStr() })
const genStrategies = ref<EmsStrategy[]>([])

function todayStr(): string {
  const d = new Date()
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
}

function openGenerate() {
  genForm.value = { planDate: todayStr() }
  genStrategies.value = []
  genDialogVisible.value = true
}

/** 电站变化 → 拉该站启用策略候选（status=1），并清空已选策略 */
async function onGenStationChange(stationId?: string) {
  genForm.value.strategyId = undefined
  genStrategies.value = []
  if (!stationId) return
  try {
    const page = await emsApi.strategyPage({ pageNo: 1, pageSize: 50, stationId, status: 1 })
    genStrategies.value = page.records
  } catch {
    genStrategies.value = [] // 候选拉取失败：策略留空走自动选择
  }
}

async function generate() {
  const { planDate, stationId, strategyId } = genForm.value
  if (!planDate) { ElMessage.error('请选择计划日期'); return }
  if (!stationId) { ElMessage.error('请选择电站'); return }
  generating.value = true
  try {
    await emsApi.planGenerate({ stationId, strategyId: strategyId || undefined, planDate })
    ElMessage.success('计划已生成')
    genDialogVisible.value = false
    await load()
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : String(e))
  } finally {
    generating.value = false
  }
}
```

- [ ] **Step 4: 模板副题名称化**

`:213` 由

```vue
{{ selected ? `${selected.planDate} · 电站 ${selected.stationId} · 策略 ${selected.strategyId}` : '加载中…' }}
```

改为

```vue
{{ selected ? `${selected.planDate} · 电站 ${stationName(selected.stationId, stations)} · 策略 ${strategyLabel(selected.strategyId)}` : '加载中…' }}
```

- [ ] **Step 5: 模板页头加「生成计划」按钮**

在「下发计划」按钮（`:216-223`）之前新增：

```vue
<el-button class="gen-btn" type="success" :disabled="generating" @click="openGenerate">生成计划</el-button>
```

- [ ] **Step 6: 模板列表电站/策略列名称化**

`:270` 由

```vue
<el-table-column prop="stationId" label="电站" width="80" />
```

改为

```vue
<el-table-column label="电站" min-width="120">
  <template #default="{ row }">{{ stationName(row.stationId, stations) }}</template>
</el-table-column>
<el-table-column label="策略" min-width="140" show-overflow-tooltip>
  <template #default="{ row }">{{ strategyLabel(row.strategyId) }}</template>
</el-table-column>
```

- [ ] **Step 7: 模板生成弹窗**

在列表卡 `</section>`（`:293`）之后、根 `</div>`（`:294`）之前新增：

```vue
<!-- 生成调度计划弹窗 -->
<el-dialog v-model="genDialogVisible" title="生成调度计划" width="480px">
  <el-form label-width="80px">
    <el-form-item label="计划日期">
      <el-date-picker v-model="genForm.planDate" type="date" value-format="YYYY-MM-DD" placeholder="选择日期" style="width: 100%" />
    </el-form-item>
    <el-form-item label="电站">
      <el-select v-model="genForm.stationId" placeholder="请选择电站" filterable clearable style="width: 100%" @change="onGenStationChange">
        <el-option v-for="s in stations" :key="s.stationId" :label="s.stationName" :value="s.stationId" />
      </el-select>
    </el-form-item>
    <el-form-item label="策略">
      <el-select v-model="genForm.strategyId" :placeholder="genForm.stationId ? '自动选择（启用中优先级最高）' : '请先选择电站'" clearable :disabled="!genForm.stationId" style="width: 100%">
        <el-option v-for="s in genStrategies" :key="s.strategyId" :label="s.strategyName" :value="s.strategyId" />
      </el-select>
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="genDialogVisible = false">取消</el-button>
    <el-button type="primary" :loading="generating" @click="generate">生成</el-button>
  </template>
</el-dialog>
```

- [ ] **Step 8: 类型检查 + 落地 + 提交**

Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0，无新增错误。

按 §命令约定落地 `EmsPlan.vue`：

```bash
git add ../../frontend/src/views/EmsPlan.vue
git commit -m "feat(ems): 计划页电站/策略名称化 + 内联生成计划弹窗"
```

---

### Task 4: 全量回归 + 提交复核

**Files:** 无新改动；仅验证。

- [ ] **Step 1: 全量类型检查**

Run: `cd "$ROOT/frontend" && npx vue-tsc --noEmit`
Expected: EXIT 0。

- [ ] **Step 2: 全量单测**

Run: `cd "$ROOT/frontend" && npm test`
Expected: 全绿（基线 + Task 1 新增 4 用例）。

- [ ] **Step 3: 提交范围复核**

Run: `git status --short`
Expected: 恰好 3 个提交（Task 1 / 2 / 3），工作区只剩两台本地端口文件 `BrokerProperties.java`、`vite.config.ts` 为未提交 M；无 `git add -A` 误带的文件。

---

### Task 5: P1 冒烟清单（spec §7，本地全栈已启动时执行）

**Files:** 无提交；仅在 `$TMP/ems-p1-smoke/smoke.mjs` 编写一次性脚本（scratch，不入库），或按下列清单手工执行。沿用 S1 冒烟脚手架（playwright-core + Edge headless、登录 admin/admin123、`waitForMsg` 精确匹配）。

> ⚠️ **S1 冒烟脚本不兼容提醒**：旧 `fillStation()`（`.el-form-item:has-text("电站 ID") input`）与「电站 ID」label 均已不存在（改为 `el-select`「电站」）。新脚本选电站用：
> ```js
> await page.click('.el-dialog .el-form-item:has-text("电站") .el-select')
> await page.waitForSelector('.el-select-dropdown__item:visible', { timeout: 5000 })
> await page.click(`.el-select-dropdown__item:visible:has-text("${stationName}")`)
> ```

| # | 步骤与断言 |
|---|-----------|
| 1 | 策略页新建：点「新增策略」→ 电站下拉显示电站名（非裸 ID），选中名称为「XX」的站；填名称/类型/config 保存成功 → 列表新增「电站」列且该行显示「XX」 |
| 2 | 复制：该行点「复制」→ 弹窗标题「新增策略」（isEdit=false）且名称=`原名称 副本`、电站下拉选中原站、类型/config 回显 → 保存 → 新行状态草稿、原策略未动；API 回读新行 `status=0`、`stationId` 与原一致 |
| 3 | 计划页：打开 `/ems/plan` → 副题形如「`日期 · 电站 <名称> · 策略 <名称>`」（无裸数字 id）；列表「电站」「策略」两列均显示名称 |
| 4 | 计划页生成（自动路径）：点「生成计划」→ 不选策略 → 生成 → toast「计划已生成」、列表刷新、新计划选中展示波形 |
| 5 | 计划页生成（显式路径）：再次点「生成计划」→ 电站选上一步同一站 → 策略下拉出现该站启用策略 → 选一条 → 生成 → 若同站同日则 toast 显示后端 CONFLICT「该电站该日期已存在计划，请勿重复生成」；换日期则成功 |
| 6 | 策略下拉按站过滤：生成弹窗电站 A → 策略候选仅 A 的启用策略；电站清空 → 策略下拉禁用 |
| 7 | 编辑回填：策略页编辑已有策略 → 电站下拉选中项为原站名称 |

若本地全栈未启动，标记「待运行时冒烟」并在回归报告中明示，不静默跳过。冒烟通过后由用户推送 origin（既定流程「我跑冒烟清单后推送 origin」）。

# P2 QA 批次实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 P2 纯前端 QA 批次：策略 / 计划 / 设备产品 / 权限 5 模块 10 项修复（ID1/2/4/10/13+tooltip/8/9/3/5 + 11-a/b 触及表单）。

**Architecture:** 纯前端。契约调整集中在纯函数层（strategyConfig / planGate / dicts，vitest 覆盖）；编辑器与视图层消费纯函数；校验错误从系统 Toast 改 `el-form-item :error` 内联红字；ID4 用共享 `planGate` 预检安全约束；ECharts 选项与窗口行 CSS 直接改。

**Tech Stack:** Vue 3.5 · Element Plus 2.9 · ECharts 5.6 · TypeScript 5.6 · Vitest 2.1 · @vue/test-utils 2.4 · happy-dom 15 · vue-tsc 2.1。

## Global Constraints

- **纯前端**：只改 `frontend/src/` 下列文件；**绝不改后端**。
- **提交纪律**：显式 pathspec 提交，**禁 `git add -A` / `git add .`**；机器本地端口文件（`backend/energy-mqtt-broker/.../BrokerProperties.java`、`frontend/vite.config.ts`）永远是 `M`，**永不提交**。
- **仓库文件 CRLF**：字符串匹配/替换用 `\r?\n` 容错正则，写回保持原文件换行风格。若 Write/Edit 拒绝仓库路径（shared-checkout 提示），用「Write 临时文件 → `cp -f` 回仓库」或 node patch 脚本（参考 `C:/Users/linwe/AppData/Local/Temp/ems-s1-smoke/patch-staleguard.mjs` 模式）。
- **11-b**：校验错误（字段缺失/格式）只走输入框下内联红字，**禁校验 Toast**；领域前置条件（约束未就绪）才用 `ElMessage.warning`。
- **11-a**：必填 form-item 加 `required`（红星）。
- **名称回退**：任何 id 查不到显示裸 id，绝不空白（`stationName` 已内置）。
- **后端契约**：`deviceTypeOptions` 值数组原样保留（`dicts.spec.ts:40-43` 依赖）；电站下拉 `label=stationName / value=stationId`；分页参数 `pageNum`（station）与 `pageNo`（EMS）勿混。
- **文案精确**：ID4 预检 warning 文案逐字为 `该电站安全约束未就绪（未配置或 SOC/功率上下限缺失），无法生成计划`；ID1/ID2 相关文案逐字为 `请至少配置一个充电或放电窗口`。
- **提交信息**：`fix(frontend):` / `feat(frontend):` / `test(frontend):` 前缀 + 中文描述。

---

### Task 1: strategyConfig 契约变更（ID2）—— parsePeakValleyConfig 去「≥1 窗口」 + 新增 validatePeakValleySaveable

**Files:**
- Modify: `frontend/src/utils/strategyConfig.ts`（删除 both-empty 拦截分支；新增保存闸函数）
- Test: `frontend/src/utils/__tests__/strategyConfig.spec.ts`

**Interfaces:**
- Consumes: `parseJsonConfig` / `validatePeakValleyConfig`（既有，不变）。
- Produces: `validatePeakValleySaveable(config: string): string[]` —— Task 5 `EmsStrategy.save` 消费；语义 = 结构校验 + 至少一个窗口。

- [ ] **Step 1: 更新测试（先改契约断言）**

在 `strategyConfig.spec.ts` 的 import 中加入 `validatePeakValleySaveable`：

```ts
import {
  parseJsonConfig,
  parsePeakValleyConfig,
  serializePeakValley,
  validatePeakValleyConfig,
  validatePeakValleySaveable,
} from '@/utils/strategyConfig'
```

替换「至少一个窗口」测试块（`it('parsePeakValleyConfig：至少一个窗口', ...)`）为：

```ts
it('parsePeakValleyConfig：空窗口数组合法（ID2 可往返结构化编辑）', () => {
  const r = parsePeakValleyConfig({ chargeWindows: [], dischargeWindows: [] })
  expect(r.ok).toBe(true)
  if (r.ok) {
    expect(r.config.chargeWindows).toHaveLength(0)
    expect(r.config.dischargeWindows).toHaveLength(0)
  }
})
```

替换「语法错与结构化错」测试内的行 `expect(validatePeakValleyConfig('{"chargeWindows":[],"dischargeWindows":[]}')[0]).toBe('请至少配置一个充电或放电窗口')` 为：

```ts
expect(validatePeakValleyConfig('{"chargeWindows":[],"dischargeWindows":[]}')).toEqual([])
```

追加两个新测试块：

```ts
it('validatePeakValleySaveable：空配置 / 空窗口 → 至少一个窗口；合法 → []', () => {
  expect(validatePeakValleySaveable('')).toEqual(['请至少配置一个充电或放电窗口'])
  expect(validatePeakValleySaveable('  ')).toEqual(['请至少配置一个充电或放电窗口'])
  expect(validatePeakValleySaveable('{"chargeWindows":[],"dischargeWindows":[]}')).toEqual(['请至少配置一个充电或放电窗口'])
  expect(validatePeakValleySaveable(validConfig)).toEqual([])
})

it('空窗口配置序列化后往返解析 ok（ID2 模式切换）', () => {
  const s = serializePeakValley({ chargeWindows: [], dischargeWindows: [] }, {})
  const back = parsePeakValleyConfig(JSON.parse(s))
  expect(back.ok).toBe(true)
  if (back.ok) expect(back.config.chargeWindows).toHaveLength(0)
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd frontend && npx vitest run src/utils/__tests__/strategyConfig.spec.ts`
Expected: FAIL ——「至少一个窗口」两处断言（`ok` 应为 `false` 但实际 `true`；`[0]` 为 `undefined`）与 `validatePeakValleySaveable is not defined`。

- [ ] **Step 3: 修改 strategyConfig.ts**

删除 `parsePeakValleyConfig` 内的 both-empty 拦截分支（`:76-78`）：

```ts
  if (chargeWindows.length === 0 && dischargeWindows.length === 0) {
    return { ok: false, error: '请至少配置一个充电或放电窗口' }
  }
```

在 `validatePeakValleyConfig` 之后新增：

```ts
/** 保存闸：结构校验（validatePeakValleyConfig）+ 至少一个窗口。空配置 = 无窗口。 */
export function validatePeakValleySaveable(config: string): string[] {
  if (!config.trim()) return ['请至少配置一个充电或放电窗口'] // 空配置 ≠ 非法 JSON，先给「至少一个窗口」
  const issues = validatePeakValleyConfig(config)
  if (issues.length) return issues
  const parsed = parseJsonConfig(config)
  if (!parsed.ok) return [parsed.error] // 理论不可达（上面已过结构校验），防御窄化
  const obj = parsed.value as { chargeWindows?: unknown[]; dischargeWindows?: unknown[] }
  if ((obj.chargeWindows?.length ?? 0) === 0 && (obj.dischargeWindows?.length ?? 0) === 0) {
    return ['请至少配置一个充电或放电窗口']
  }
  return []
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd frontend && npx vitest run src/utils/__tests__/strategyConfig.spec.ts`
Expected: PASS（全部用例）。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/strategyConfig.ts frontend/src/utils/__tests__/strategyConfig.spec.ts
git commit -m "fix(frontend): parsePeakValleyConfig 去「≥1 窗口」限制，新增 validatePeakValleySaveable（ID2 模式往返）"
```

---

### Task 2: dicts 中文映射（ID9/ID5 基础）

**Files:**
- Modify: `frontend/src/utils/dicts.ts`
- Test: `frontend/src/utils/__tests__/dicts.spec.ts`

**Interfaces:**
- Consumes: 无（纯新增）。
- Produces: `DEVICE_TYPE_TEXT` / `deviceTypeText(t?: string | null): string` / `deviceTypeLabel(t: string): string` / `RESOURCE_TYPE_TEXT` / `resourceTypeText(t?: string | null): string` —— Task 7（Product）、Task 8（Device）、Task 9（SystemPerm）消费。`deviceTypeOptions` 保持原样。

- [ ] **Step 1: 写失败测试**

在 `dicts.spec.ts` import 中加入 `deviceTypeText, deviceTypeLabel, resourceTypeText`，追加：

```ts
it('设备类型中文', () => {
  expect(deviceTypeText('PCS')).toBe('变流器 PCS')
  expect(deviceTypeText('ENERGY_CABINET')).toBe('储能柜')
  expect(deviceTypeText('NOPE')).toBe('NOPE')
  expect(deviceTypeText(null)).toBe('—')
  expect(deviceTypeLabel('PCS')).toBe('变流器 PCS (PCS)')
})

it('资源类型中文', () => {
  expect(resourceTypeText('DEVICE')).toBe('设备')
  expect(resourceTypeText('X')).toBe('X')
  expect(resourceTypeText(null)).toBe('不限')
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd frontend && npx vitest run src/utils/__tests__/dicts.spec.ts`
Expected: FAIL —— `deviceTypeText is not defined`。

- [ ] **Step 3: 实现**

在 `dicts.ts` 的 `deviceTypeOptions`（`:38`）之后追加：

```ts
export const DEVICE_TYPE_TEXT: Record<string, string> = {
  ENERGY_CABINET: '储能柜',
  BATTERY_CLUSTER: '电池簇',
  PCS: '变流器 PCS',
  BMS: '电池管理系统 BMS',
  EMS: '能量管理系统 EMS',
  EDGE_GW: '边缘网关',
}
/** 设备类型中文（未知回退原 code；空回退 '—'） */
export function deviceTypeText(t?: string | null): string {
  return t ? (DEVICE_TYPE_TEXT[t] ?? t) : '—'
}
/** 下拉 label：中文 (CODE) */
export function deviceTypeLabel(t: string): string {
  return `${DEVICE_TYPE_TEXT[t] ?? t} (${t})`
}

export const RESOURCE_TYPE_TEXT: Record<string, string> = {
  DEVICE: '设备',
  STRATEGY: '策略',
  ALARM: '告警',
  STATION: '电站',
}
/** 资源类型中文（未知回退原 code；空回退「不限」） */
export function resourceTypeText(t?: string | null): string {
  return t ? (RESOURCE_TYPE_TEXT[t] ?? t) : '不限'
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd frontend && npx vitest run src/utils/__tests__/dicts.spec.ts`
Expected: PASS（新增 + 既有 `deviceTypeOptions` 断言）。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/dicts.ts frontend/src/utils/__tests__/dicts.spec.ts
git commit -m "feat(frontend): 设备类型/资源类型中文映射（ID9/ID5）"
```

---

### Task 3: planGate 安全约束预检（ID4 基础）

**Files:**
- Create: `frontend/src/utils/planGate.ts`
- Create: `frontend/src/utils/__tests__/planGate.spec.ts`

**Interfaces:**
- Consumes: `emsApi.constraintGet`（`frontend/src/api/ems.ts:30`，`GET /api/ems/constraint?stationId=`）、`EmsConstraint`（`models.ts:208`）。
- Produces: `hasFullConstraint(c: EmsConstraint | null | undefined): boolean` / `ensureConstraint(stationId: string): Promise<EmsConstraint | null>` / `constraintReady(stationId: string): Promise<boolean>` —— Task 5（EmsStrategy.generatePlan）、Task 6（EmsPlan.generate）消费。

- [ ] **Step 1: 写失败测试**

创建 `frontend/src/utils/__tests__/planGate.spec.ts`：

```ts
import { describe, expect, it, vi } from 'vitest'
import { emsApi } from '@/api/ems'
import { ensureConstraint, hasFullConstraint, constraintReady } from '@/utils/planGate'
import type { EmsConstraint } from '@/types/models'

vi.mock('@/api/ems', () => ({ emsApi: { constraintGet: vi.fn() } }))
const mockGet = vi.mocked(emsApi.constraintGet)

const full = {
  constraintId: 'c1', tenantId: 't1', stationId: '1', status: 1,
  socMin: 10, socMax: 90, chargePowerMax: 200, dischargePowerMax: 200,
  tempMax: 60, voltageMax: null, currentMax: null, safetyEnvelope: null,
  createTime: '', updateTime: '',
} as unknown as EmsConstraint

describe('planGate', () => {
  it('hasFullConstraint：null/undefined → false', () => {
    expect(hasFullConstraint(null)).toBe(false)
    expect(hasFullConstraint(undefined)).toBe(false)
  })

  it('hasFullConstraint：4 字段任一 null → false', () => {
    expect(hasFullConstraint({ ...full, socMin: null })).toBe(false)
    expect(hasFullConstraint({ ...full, socMax: null })).toBe(false)
    expect(hasFullConstraint({ ...full, chargePowerMax: null })).toBe(false)
    expect(hasFullConstraint({ ...full, dischargePowerMax: null })).toBe(false)
  })

  it('hasFullConstraint：4 字段全非空 → true', () => {
    expect(hasFullConstraint(full)).toBe(true)
  })

  it('ensureConstraint：成功 → 行；失败 → null', async () => {
    mockGet.mockResolvedValue(full)
    await expect(ensureConstraint('1')).resolves.toBe(full)
    mockGet.mockRejectedValue(new Error('boom'))
    await expect(ensureConstraint('1')).resolves.toBeNull()
  })

  it('constraintReady：齐全 → true；失败/缺行 → false', async () => {
    mockGet.mockResolvedValue(full)
    await expect(constraintReady('1')).resolves.toBe(true)
    mockGet.mockRejectedValue(new Error('boom'))
    await expect(constraintReady('1')).resolves.toBe(false)
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd frontend && npx vitest run src/utils/__tests__/planGate.spec.ts`
Expected: FAIL —— `Cannot find module '@/utils/planGate'`。

- [ ] **Step 3: 实现**

创建 `frontend/src/utils/planGate.ts`：

```ts
/**
 * 计划生成前置：安全约束就绪预检。
 * 后端契约：EmsPlanService 仅要求 constraint 行存在（缺失抛「未配置安全约束」）；
 * SafetyEnvelopeValidator 对 socMin/socMax/chargePowerMax/dischargePowerMax 取 doubleValue()（null→NPE）。
 * 前端预检与后端闸对齐：行存在 + 4 字段非空。
 */
import { emsApi } from '@/api/ems'
import type { EmsConstraint } from '@/types/models'

/** 约束是否满足生成计划的必要条件（行存在 + 4 字段非空） */
export function hasFullConstraint(c: EmsConstraint | null | undefined): boolean {
  if (!c) return false
  return c.socMin != null && c.socMax != null && c.chargePowerMax != null && c.dischargePowerMax != null
}

/** 查询该站约束；查询失败/缺行 → null（不抛，由调用方决定提示） */
export async function ensureConstraint(stationId: string): Promise<EmsConstraint | null> {
  try {
    return await emsApi.constraintGet(stationId)
  } catch {
    return null
  }
}

/** 预检便捷入口：true = 就绪 */
export async function constraintReady(stationId: string): Promise<boolean> {
  return hasFullConstraint(await ensureConstraint(stationId))
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd frontend && npx vitest run src/utils/__tests__/planGate.spec.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/planGate.ts frontend/src/utils/__tests__/planGate.spec.ts
git commit -m "feat(frontend): 计划生成安全约束预检 planGate（ID4）"
```

---

### Task 4: StrategyConfigEditor（ID1 空配置不报错 + ID10 窗口行布局）

**Files:**
- Modify: `frontend/src/components/StrategyConfigEditor.vue`
- Create: `frontend/src/components/__tests__/StrategyConfigEditor.spec.ts`

**Interfaces:**
- Consumes: `parseJsonConfig`（既有）。
- Produces: 无对外新接口；ID1（空配置不再显示「不是合法 JSON」）、ID10-UI（窗口行自适应容器）。

- [ ] **Step 1: 写失败测试**

创建 `frontend/src/components/__tests__/StrategyConfigEditor.spec.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { mount } from '@vue/test-utils'
import ElementPlus from 'element-plus'
import StrategyConfigEditor from '@/components/StrategyConfigEditor.vue'

/** stub 掉重控件（time-picker / input-number），happy-dom 下仅断言结构类名，避免 EP 内部渲染抖动 */
function mountEditor(modelValue: string, strategyType: string) {
  return mount(StrategyConfigEditor, {
    props: { modelValue, strategyType },
    global: {
      plugins: [ElementPlus],
      stubs: {
        ElTimePicker: true, ElInputNumber: true,
        'el-time-picker': true, 'el-input-number': true,
      },
    },
  })
}

describe('StrategyConfigEditor', () => {
  it('ID1：非峰谷 + 空配置 → 无「不是合法 JSON」错误态', () => {
    const wrapper = mountEditor('', '')
    expect(wrapper.find('.json-state.err').exists()).toBe(false)
    expect(wrapper.text()).not.toContain('配置不是合法 JSON')
  })

  it('ID1：峰谷 + 空配置 → 结构化空表、无阻断警报', () => {
    const wrapper = mountEditor('', 'PEAK_VALLEY')
    expect(wrapper.findAll('.window-row')).toHaveLength(0)
    expect(wrapper.find('.block-alert').exists()).toBe(false)
  })

  it('峰谷 + 非空配置 → 结构化表单回显窗口数', () => {
    const config = JSON.stringify({
      chargeWindows: [{ start: '02:00', end: '06:00', powerLimit: 100 }],
      dischargeWindows: [],
    })
    const wrapper = mountEditor(config, 'PEAK_VALLEY')
    expect(wrapper.findAll('.window-row')).toHaveLength(1)
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd frontend && npx vitest run src/components/__tests__/StrategyConfigEditor.spec.ts`
Expected: FAIL —— 第 1 条 `.json-state.err` 存在（当前实现空配置误报）。

- [ ] **Step 3: 实现**

**ID1**：`jsonErrorOf`（`:67-70`）改为空配置不报错：

```ts
function jsonErrorOf(config: string): string {
  if (!config.trim()) return '' // 空配置不是错误（新增/非峰谷初始态）
  const r = parseJsonConfig(config)
  return r.ok ? '' : r.error
}
```

**ID10-UI**：模板两处时间选择器 `style="width: 110px"`（`:216`、`:218`）→ `style="width: 100px"`；input-number `style="width: 130px"`（`:219`）→ `style="width: 120px"`。`.window-row`（`:291-296`）CSS：

```css
.window-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 6px;
  margin-bottom: 6px;
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd frontend && npx vitest run src/components/__tests__/StrategyConfigEditor.spec.ts`
Expected: PASS（3 条）。若 happy-dom 下 `el-time-picker` 渲染告警但不影响断言，可接受；断言目标仅 `.window-row` 计数与错误态类名。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/StrategyConfigEditor.vue frontend/src/components/__tests__/StrategyConfigEditor.spec.ts
git commit -m "fix(frontend): 策略配置编辑器空配置不误报 + 窗口行自适应（ID1/ID10-UI）"
```

---

### Task 5: EmsStrategy（ID10 必填校验内联 + ID4 生成预检 + 11-a 红星）

**Files:**
- Modify: `frontend/src/views/EmsStrategy.vue`

**Interfaces:**
- Consumes: `validatePeakValleySaveable`（Task 1）、`parseJsonConfig`（既有）、`constraintReady`（Task 3）。
- Produces: 无新接口；save() 校验改内联 `errs`；`generatePlan` 加约束预检。

- [ ] **Step 1: 改脚本逻辑**

`import`（`:9`）改 `import { parseJsonConfig, validatePeakValleySaveable } from '@/utils/strategyConfig'`（替换 `validatePeakValleyConfig`）；新增 `import { constraintReady } from '@/utils/planGate'`。

在 `editing`/`isEdit` 之后新增：

```ts
/** 保存前置校验错误（内联红字，11-b：校验不走系统 Toast） */
const errs = ref<Record<string, string>>({})
watch(
  () => [editing.value.strategyName, editing.value.strategyType, editing.value.stationId, editing.value.config],
  () => { errs.value = {} },
)
```

`save()`（`:109-149`）开头替换为（保留下方 payload/API 部分不动）：

```ts
async function save() {
  errs.value = {}
  const e: Record<string, string> = {}
  if (!editing.value.strategyName?.trim()) e.strategyName = '请填写策略名称'
  if (!editing.value.strategyType) e.strategyType = '请选择策略类型'
  if (!editing.value.stationId) e.stationId = '请选择电站'
  const raw = editing.value.config ?? ''
  if (!e.strategyType) {
    if (editing.value.strategyType === 'PEAK_VALLEY') {
      const issues = validatePeakValleySaveable(raw)
      if (issues.length) e.config = issues[0]
    } else if (!raw.trim()) {
      e.config = '请填写配置 JSON' // 非峰谷空配置：非「非法 JSON」，明确提示
    } else {
      const r = parseJsonConfig(raw)
      if (!r.ok) e.config = r.error
    }
  }
  if (Object.keys(e).length) { errs.value = e; return }
  // —— 以下保持不变：非可生成类型 warning、payload 构造、strategyCreate/Update、toast、load ——
```

`generatePlan`（`:176-189`）在 `isStrategyGeneratable` 检查后、构造 `planDate` 前插入：

```ts
  if (!(await constraintReady(row.stationId))) {
    ElMessage.warning('该电站安全约束未就绪（未配置或 SOC/功率上下限缺失），无法生成计划')
    return
  }
```

- [ ] **Step 2: 改模板（11-a 红星 + 内联错误）**

```html
<el-form-item label="策略名称" required :error="errs.strategyName">
<el-form-item label="策略类型" required :error="errs.strategyType">
<el-form-item label="电站" required :error="errs.stationId">
<el-form-item label="配置 JSON" :error="errs.config">
```

（各自保留现有子控件不变。）

- [ ] **Step 3: 类型检查**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: EXIT 0。`parseJsonConfig` / `validatePeakValleySaveable` / `constraintReady` / `errs` 全部有引用且类型正确；确认无未用 import（`validatePeakValleyConfig` 已从 import 移除）。

- [ ] **Step 4: 回归单测**

Run: `cd frontend && npx vitest run`
Expected: 既有单测全绿（本任务不动纯函数，仅视图消费）。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/EmsStrategy.vue
git commit -m "fix(frontend): 策略页必填校验改内联红字 + 生成计划约束预检（ID10/ID4/11-a）"
```

---

### Task 6: EmsPlan（ID13 图表留白 + tooltip 精度 + 生成弹窗校验 + ID4 预检）

**Files:**
- Modify: `frontend/src/views/EmsPlan.vue`

**Interfaces:**
- Consumes: `constraintReady`（Task 3）、组件内 `round1`（既有 `:55-57`）。
- Produces: 无新接口。

- [ ] **Step 1: 改图表选项**

`renderChart`（`:153-204`）：`grid` 的 `top: 20` → `top: 48`；`tooltip` 加 formatter：

```ts
tooltip: {
  trigger: 'axis',
  axisPointer: { type: 'line' },
  formatter(params: unknown) {
    const list = params as Array<{ axisValueLabel: string; seriesName: string; value: number | [number, number] }>
    const head = list[0]?.axisValueLabel ?? ''
    const rows = list.map((p) => {
      const v = typeof p.value === 'number' ? p.value : (p.value as [number, number] | undefined)?.[1] ?? 0
      return p.seriesName === 'SOC' ? `SOC：${round1(v)} %` : `${p.seriesName}：${round1(v)} kW`
    })
    return [head, ...rows].join('<br/>')
  },
},
```

- [ ] **Step 2: 生成弹窗内联校验 + ID4 预检**

新增：

```ts
import { constraintReady } from '@/utils/planGate'
```

在 `genDialogVisible` 旁新增 `const genErrs = ref<Record<string, string>>({})`；新增清空 watch：

```ts
watch([() => genForm.value.planDate, () => genForm.value.stationId], () => { genErrs.value = {} })
```

`generate()`（`:252-267`）开头替换：

```ts
async function generate() {
  genErrs.value = {}
  const e: Record<string, string> = {}
  const { planDate, stationId } = genForm.value
  if (!planDate) e.planDate = '请选择计划日期'
  if (!stationId) e.stationId = '请选择电站'
  if (Object.keys(e).length) { genErrs.value = e; return }
  if (!(await constraintReady(stationId))) {
    ElMessage.warning('该电站安全约束未就绪（未配置或 SOC/功率上下限缺失），无法生成计划')
    return
  }
  // —— 以下 generating/planGenerate/load/finally 不变 ——
```

模板计划日期 / 电站 form-item（`:376-383`）加 `required` + `:error`：

```html
<el-form-item label="计划日期" required :error="genErrs.planDate">
<el-form-item label="电站" required :error="genErrs.stationId">
```

- [ ] **Step 3: 类型检查**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: EXIT 0。确认 `watch` 已 import（脚本顶部目前 `import { computed, onMounted, ref } from 'vue'` → 需补 `watch`）。

- [ ] **Step 4: 回归单测**

Run: `cd frontend && npx vitest run`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/EmsPlan.vue
git commit -m "fix(frontend): 计划图表顶缘留白 + tooltip 精度 + 生成弹窗内联校验与约束预检（ID13/ID4/11-a）"
```

---

### Task 7: Product（ID9 中文 + 11-b 表单内联校验）

**Files:**
- Modify: `frontend/src/views/Product.vue`

**Interfaces:**
- Consumes: `deviceTypeLabel` / `deviceTypeText`（Task 2）。
- Produces: 无新接口。

- [ ] **Step 1: 改脚本**

`import`（`:6`）加 `deviceTypeLabel, deviceTypeText`；新增 `errs` ref + 清空 watch：

```ts
const errs = ref<Record<string, string>>({})
watch(
  () => [form.value.productKey, form.value.productName, form.value.deviceType],
  () => { errs.value = {} },
)
```

`save()`（`:52-64`）开头替换：

```ts
async function save() {
  errs.value = {}
  const e: Record<string, string> = {}
  if (!form.value.productKey?.trim()) e.productKey = '请输入 productKey'
  if (!form.value.productName?.trim()) e.productName = '请输入产品名称'
  if (!form.value.deviceType) e.deviceType = '请选择设备类型'
  if (Object.keys(e).length) { errs.value = e; return }
  // —— 以下 create/update/toast/load 不变 ——
```

`ref` 已 import；`watch` 需补进 vue import（当前 `import { onMounted, ref } from 'vue'`）。

- [ ] **Step 2: 改模板**

- 筛选设备类型下拉（`:143`）：option label → `deviceTypeLabel(t)`。
- 表单设备类型下拉（`:206`）：option label → `deviceTypeLabel(t)`。
- 表格类型列（`:166-167`）：`<el-tag ...>{{ deviceTypeText(row.deviceType) }}</el-tag>`。
- 三个必填 form-item 加 `:error`：productKey / productName / deviceType。

- [ ] **Step 3: 类型检查**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: EXIT 0。

- [ ] **Step 4: 回归单测**

Run: `cd frontend && npx vitest run`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/Product.vue
git commit -m "feat(frontend): 产品页设备类型中文 + 表单内联校验（ID9/11-b）"
```

---

### Task 8: Device（ID3 电站名称化 + ID8 类型联动 + ID9 中文 + 11-b）

**Files:**
- Modify: `frontend/src/views/Device.vue`

**Interfaces:**
- Consumes: `loadStations` / `stationName`（`utils/stationDict.ts`，P1）、`deviceTypeText` / `deviceTypeLabel`（Task 2）、`Station` 类型。
- Produces: 无新接口。

- [ ] **Step 1: 改脚本**

- vue import（`:2`）：`import { computed, onMounted, ref, watch } from 'vue'`。
- 类型 import（`:7`）：加 `Station`。
- dicts import（`:8`）：加 `deviceTypeLabel, deviceTypeText`。
- 新增 `import { loadStations, stationName } from '@/utils/stationDict'`。
- 新增状态与加载：

```ts
const stations = ref<Station[]>([])
const errs = ref<Record<string, string>>({})
const narrowedTypes = computed(() => {
  const p = products.value.find((x) => x.productKey === form.value.productKey)
  return p?.deviceType ? [p.deviceType] : deviceTypeOptions
})
async function loadStationOptions() {
  try { stations.value = await loadStations() } catch { /* 名称回退裸 id，不阻断 */ }
}
watch(
  () => [form.value.deviceName, form.value.productKey, form.value.deviceType],
  () => { errs.value = {} },
)
```

- `onMounted`（`:160`）加 `void loadStationOptions()`。
- `save()`（`:69-104`）开头替换（移除两个 `ElMessage.warning` 校验分支）：

```ts
async function save() {
  errs.value = {}
  const e: Record<string, string> = {}
  if (!form.value.deviceName?.trim()) e.deviceName = '设备名必填'
  else if (form.value.deviceName.includes('_') || form.value.deviceName.includes('&')) e.deviceName = 'deviceName 禁止包含 _ 或 &（接入契约）'
  if (!form.value.productKey) e.productKey = '请选择产品'
  if (!form.value.deviceType) e.deviceType = '请选择设备类型'
  if (Object.keys(e).length) { errs.value = e; return }
  // —— 以下 isEdit 分支 / create / toast / load 不变 ——
```

- [ ] **Step 2: 改模板**

- 筛选设备类型下拉（`:183-184`）：label → `deviceTypeLabel(t)`。
- 筛选「电站 ID」`el-input`（`:192-194`）→ 名称下拉：

```html
<el-form-item label="电站">
  <el-select v-model="query.stationId" clearable filterable placeholder="全部" style="width: 170px">
    <el-option v-for="s in stations" :key="s.stationId" :label="s.stationName" :value="s.stationId" />
  </el-select>
</el-form-item>
```

- 表格类型列（`:212`）→ `<el-table-column label="类型" width="130"><template #default="{ row }">{{ deviceTypeText(row.deviceType) }}</template></el-table-column>`。
- 表格电站列（`:218-220`）→

```html
<el-table-column label="电站" min-width="110" show-overflow-tooltip>
  <template #default="{ row }">{{ stationName(row.stationId, stations) }}</template>
</el-table-column>
```

- 表单 deviceName（`:244`）加 `:error="errs.deviceName"`；产品（`:247`）加 `:error="errs.productKey"`。
- 表单设备类型（`:252-256`）→

```html
<el-form-item label="设备类型" required :error="errs.deviceType">
  <el-select v-model="form.deviceType" style="width: 100%" :disabled="isEdit || !!form.productKey">
    <el-option v-for="t in narrowedTypes" :key="t" :label="deviceTypeLabel(t)" :value="t" />
  </el-select>
</el-form-item>
```

- 表单「电站 ID」`el-input`（`:262-264`）→

```html
<el-form-item label="电站">
  <el-select v-model="form.stationId" clearable filterable placeholder="无" style="width: 100%">
    <el-option v-for="s in stations" :key="s.stationId" :label="s.stationName" :value="s.stationId" />
  </el-select>
</el-form-item>
```

- 详情类型（`:291`）→ `{{ deviceTypeText(detail.deviceType) }}`；详情电站（`:293`）→ `{{ stationName(detail.stationId, stations) || '—' }}`（`stationName` 对 null 返回 `''`，保留原有 `'—'` 兜底）。

- [ ] **Step 3: 类型检查**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: EXIT 0。确认无未用 import、`narrowedTypes` 类型（`string[]`）、`query.stationId`（`string | undefined`）与 el-select 绑定兼容。

- [ ] **Step 4: 回归单测**

Run: `cd frontend && npx vitest run`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/Device.vue
git commit -m "feat(frontend): 设备页电站名称下拉 + 设备类型联动与中文 + 表单内联校验（ID3/ID8/ID9/11-b）"
```

---

### Task 9: SystemPerm（ID5 资源类型中文 + Tooltip 说明）

**Files:**
- Modify: `frontend/src/views/SystemPerm.vue`

**Interfaces:**
- Consumes: `resourceTypeText`（Task 2）。
- Produces: 无新接口。

- [ ] **Step 1: 改脚本与模板**

`import`（`:6`）加 `resourceTypeText`。资源类型 form-item（`:135-139`）替换为：

```html
<el-form-item>
  <template #label>
    <span>资源类型</span>
    <el-tooltip content="数据权限预留字段：当前鉴权按权限编码判断，此字段暂不生效" placement="top">
      <span class="res-tip">ⓘ</span>
    </el-tooltip>
  </template>
  <el-select v-model="form.resourceType" clearable placeholder="不限" style="width: 100%">
    <el-option v-for="t in ['DEVICE', 'STRATEGY', 'ALARM', 'STATION']" :key="t" :label="`${resourceTypeText(t)} (${t})`" :value="t" />
  </el-select>
</el-form-item>
```

style 块追加：

```css
.res-tip {
  cursor: help;
  margin-left: 4px;
  font-size: 12px;
}
```

- [ ] **Step 2: 类型检查**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: EXIT 0。

- [ ] **Step 3: 回归单测**

Run: `cd frontend && npx vitest run`
Expected: 全绿。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/views/SystemPerm.vue
git commit -m "feat(frontend): 菜单权限资源类型中文 + 预留字段说明（ID5）"
```

---

### Task 10: 全量回归验证（无提交）

**Files:**
- 无代码改动；验证产物写入 SDD workspace `progress.md`。

- [ ] **Step 1: vue-tsc**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: EXIT 0。

- [ ] **Step 2: vitest 全量**

Run: `cd frontend && npx vitest run`
Expected: 全绿（既有 74 项 + Task 1-4 新增，估算 80+）。

- [ ] **Step 3: 提交范围核查**

Run: `git log --oneline <BASE>..HEAD`（BASE = 本计划起点的 commit `6a32323` 之后首个实现 commit 之前）
Expected: 恰好 Task 1-9 的 9 个实现提交 + spec 提交；工作区 `git status` 仅两台机器本地端口文件 `M`（`BrokerProperties.java` / `vite.config.ts`），无其他游离改动。

- [ ] **Step 4: 记录**

将回归结果追加到 `.superpowers/sdd/2026-08-09-qa-p2/progress.md`（vue-tsc / vitest / 提交范围三项结论）。

> **冒烟由协调者执行**：SDD 全部任务完成后，协调者运行 P2 冒烟脚本（`C:/Users/linwe/AppData/Local/Temp/ems-s1-smoke/` 下新建 `smoke-p2.mjs`，清单见 spec §7 的 10 项），结果写入 smoke-report 并汇报；通过后用户推 origin。

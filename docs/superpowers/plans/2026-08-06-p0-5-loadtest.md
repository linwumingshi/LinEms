# P0-5 首轮真实压测与 P99 基线 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用现有 stress 工具 + 新编排脚本跑通首轮真实压测，产出四档吞吐基线（4万/8万/16万/32万 msg/s）、控制链路 P99 基线、故障演练 01/02/05 结果，写入 P0-5 压测基线报告，并更新 Phase9 文档。

**Architecture:** 方案 A——复用现有 `test/stress` 工具与 `test/drill` 演练脚本，新增一个编排脚本 `test/stress/run-baseline.sh`（零 Java 改动）。脚本执行：前置检查 → TDengine 初始化（单节点变体 DDL）→ seed 16000 → 四档吞吐压测（每档前后外部观测快照：Broker stats / Kafka LAG / TDengine 落库计数）→ 判停找峰值 → 演练 05/01/02 → 汇总 summary.json。报告文档独立产出。

**Tech Stack:** Bash（Git Bash）、现有 `stress.jar`（Java）、Docker（Kafka 4.1.2 / TDengine 3.3.1）、原生 Nacos 3.1.0 / Redis / MySQL、TDengine REST。

## Global Constraints

- **Maven 构建**：`-Dmaven.repo.local="/d/Program Files/maven-repo"`（本次不新增依赖、不改 Java，stress.jar 已构建于 `test/stress/target/stress.jar`）。
- **Nacos**：127.0.0.1:8848，creds `nacos/QAQ123qaq`，group `ENERGY`，namespace `public`；Nacos 先启动。
- **JWT HS384**，secret 在 Nacos `energy-shared.yaml`；仓库零明文。
- **不改任何后端/前端代码**；`GlobalAuthFilter` 禁止修改；**本次只建基线+报告，不修瓶颈**（G-1 tsdb 不自动建库、G-2 DDL 语法不兼容只记录，不动 Java）。
- **唯一允许的代码改动**：`test/drill/lib.sh` 的 `kafka_cmd` 容器脚本路径（bitnami→apache 镜像适配，属于环境适配修复，非瓶颈修复）。
- **Bash env 文件**：值含 `&` 须单引号。
- **Git Bash 进程管理**：停 Java 服务用 `kill`（jps 取 pid）；不要用 `ps` 判断 Windows java.exe。
- **TDengine REST**：raw SQL body + `-u root:taosdata`（非 JSON 包裹）；SQL 含空格时用引号。
- **Kafka 容器**：apache/kafka:4.1.2，脚本路径 `/opt/kafka/bin/`（非 bitnami）。
- **无 git 仓库**：不 commit；验证用 grep / 文件存在 / 输出断言。

---

### Task 0: 适配 lib.sh 的 kafka_cmd 镜像路径

**Files:**
- Modify: `test/drill/lib.sh:146`（`kafka_cmd` 函数）

**Interfaces:**
- Consumes: `test/drill/lib.sh` 现有函数（`compose`、`port_open` 等）
- Produces: 修正后的 `kafka_cmd`，供 `kafka_group_describe`/`kafka_group_lag_sum`（Task 1 的 run-baseline.sh 与演练 01 依赖）

**Why:** `kafka_cmd` 当前硬编码 `/opt/bitnami/kafka/bin/`（bitnami 镜像路径）。P0-5 环境 Kafka 已切 apache/kafka:4.1.2 官方镜像，容器内实际路径为 `/opt/kafka/bin/`（已实证 `ls /opt/kafka/bin/kafka-consumer-groups.sh` 存在）。不修复则 Task 1 的 Kafka LAG 观测与演练 01 全部失败——这是环境适配，非瓶颈修复。

- [ ] **Step 1: 修改 `kafka_cmd` 容器脚本路径**

Edit `test/drill/lib.sh` line 146：
```bash
kafka_cmd() { # kafka-脚本名 args...
  compose exec -T kafka "/opt/kafka/bin/$1" --bootstrap-server 127.0.0.1:9092 "${@:2}"
}
```
（仅替换 `/opt/bitnami/kafka/bin/` → `/opt/kafka/bin/`，其余不变。）

- [ ] **Step 2: 验证 LAG 查询可用**

Run（Git Bash，仓库根）：
```bash
source test/drill/lib.sh
kafka_group_describe energy-access-uplink | head -5
```
Expected: 输出该消费组的 describe 头部（含 GROUP / TOPIC / PARTITION / LAG 列头），非 `bash: line 1: ... No such file or directory`。

- [ ] **Step 3: 验证 LAG 求和非空**

```bash
source test/drill/lib.sh
kafka_group_lag_sum energy-access-uplink
```
Expected: 输出整数（可为 0，但命令成功、无报错）。

---

### Task 1: 编写编排脚本 run-baseline.sh（含 TDengine 初始化）

**Files:**
- Create: `test/stress/run-baseline.sh`

**Interfaces:**
- Consumes: `test/stress/target/stress.jar`；`test/drill/lib.sh` 的 `ROOT`/`STRESS_JAR`/`log`/`info`/`die`/`port_open`/`stress`/`broker_field`/`kafka_group_lag_sum`/`compose`；`deploy/env/local.env`（经 lib.sh 加载，提供 MYSQL_PASSWORD）
- Produces: 可执行编排脚本；脚本内部产出 `test/stress/results/<run-id>/` 数据目录

**说明：** 脚本分两部分实现。本 Task 完成**骨架 + 前置检查 + TDengine 初始化 + seed**；Task 2 补四档吞吐压测与外部观测；Task 3 补演练与汇总。分阶段便于每步独立验证。

- [ ] **Step 1: 写脚本骨架 + 前置检查 + TDengine 初始化 + seed**

创建 `test/stress/run-baseline.sh`：

```bash
#!/usr/bin/env bash
# =====================================================================
# P0-5 首轮真实压测 + P99 基线编排
# 依赖：test/stress/target/stress.jar（已构建）；test/drill/lib.sh
# 前置：全栈已启动（Nacos/Kafka/Redis/MySQL/TDengine + 11 服务）；
#       MYSQL_PASSWORD 由 lib.sh 加载 deploy/env/local.env 注入
# 用法：bash test/stress/run-baseline.sh [--max-tiers 4] [--duration 60]
# =====================================================================
set -uo pipefail
source "$(dirname "$0")/../drill/lib.sh"

RUN_ID="$(date +%Y%m%d-%H%M%S)"
RESULTS_DIR="${ROOT}/test/stress/results/${RUN_ID}"
mkdir -p "$RESULTS_DIR"

MAX_TIERS="${MAX_TIERS:-4}"     # 最多压到第几档（1-4）
DURATION="${DURATION:-60}"      # 每档时长（秒）
TD_HTTP="http://127.0.0.1:6041/rest/sql"
TD_USER="root"; TD_PASS="taosdata"

log "========== P0-5 基线压测 run-baseline 开始 =========="
log "运行目录: ${RESULTS_DIR}"

# ---------------------------------------------------------------
# 工具函数：TDengine REST（raw SQL body）
# ---------------------------------------------------------------
td_sql() { # sql → JSON 输出
  curl -s -m 15 -u "${TD_USER}:${TD_PASS}" -d "$1" "$TD_HTTP"
}

td_count() { # 库.表 → 行数（解析 count(*) 结果 data[0][0]）
  td_sql "SELECT count(*) FROM $1" | sed -n 's/.*"data":\[\[\([0-9][0-9]*\)\]\].*/\1/p'
}

# ---------------------------------------------------------------
# 0. 前置检查（fail-fast）
# ---------------------------------------------------------------
log "[1/6] 前置检查 ..."
for item in "nacos:8848" "kafka:9092" "broker-mgmt:8082" "tdengine:6041" "mysql:3306"; do
  host=${item%:*}; port=${item##*:}
  if ! port_open 127.0.0.1 "$port"; then
    die "${host}:${port} 未监听（${item}）——请先启动全栈"
  fi
done
if [ ! -f "$STRESS_JAR" ]; then
  die "缺少压测工具 ${STRESS_JAR}（先 cd test/stress && mvn package）"
fi
info "[1/6] 前置检查通过"

# ---------------------------------------------------------------
# 1. TDengine 初始化（幂等；G-2 单节点变体 DDL）
# ---------------------------------------------------------------
log "[2/6] TDengine 初始化（G-2：DURATION/WAL_FSYNC_PERIOD/REPLICA 1/VGROUPS 8）..."
if ! td_sql "SHOW DATABASES" | grep -q iot_tsdb_raw; then
  td_sql "CREATE DATABASE IF NOT EXISTS iot_tsdb_raw PRECISION 'ms' KEEP 365 DURATION 10 BUFFER 256 WAL_LEVEL 2 WAL_FSYNC_PERIOD 0 REPLICA 1 VGROUPS 8"
  # TDengine 建库有 dropping 状态窗口，重试至成功
  for i in 1 2 3 4 5; do
    if td_sql "SHOW DATABASES" | grep -q iot_tsdb_raw; then break; fi
    info "  等待建库生效（${i}）..."; sleep 2
  done
fi
td_sql "CREATE STABLE IF NOT EXISTS iot_tsdb_raw.st_prop_snd_ess_pcs (ts TIMESTAMP, msg_id NCHAR(64), data_type NCHAR(16), soc FLOAT, voltage FLOAT, current FLOAT, power FLOAT, temp FLOAT, run_mode INT) TAGS (device_id NCHAR(64), station_id NCHAR(32), enterprise_id NCHAR(32), product_key NCHAR(64))"
td_sql "CREATE STABLE IF NOT EXISTS iot_tsdb_raw.st_event (ts TIMESTAMP, event_id NCHAR(64), event_name NCHAR(64), severity INT, code NCHAR(32), payload JSON) TAGS (device_id NCHAR(64), station_id NCHAR(32), enterprise_id NCHAR(32), product_key NCHAR(64))"
info "[2/6] TDengine 就绪（库 + STABLE）"

# ---------------------------------------------------------------
# 2. seed 16000 台设备（幂等 INSERT IGNORE）
# ---------------------------------------------------------------
log "[3/6] seed 16000 台设备（snd_ess_pcs）..."
stress seed --count 16000 --product snd_ess_pcs --secret-base sanduo-stress \
  --user root --password "${MYSQL_PASSWORD:-}" \
  > "${RESULTS_DIR}/seed.log" 2>&1 || die "seed 失败，见 ${RESULTS_DIR}/seed.log"
info "[3/6] seed 完成"
```

- [ ] **Step 2: 语法检查**

Run：`bash -n test/stress/run-baseline.sh`
Expected: 无输出，退出码 0。

- [ ] **Step 3: 手动分步验证（前置检查 + TDengine 初始化 + seed）**

Run（Git Bash，仓库根；MYSQL_PASSWORD 从 local.env 注入）：
```bash
bash -c 'source deploy/env/local.env && MYSQL_PASSWORD="$MYSQL_PASSWORD" bash -c "source test/drill/lib.sh; port_open 127.0.0.1 8848 && echo Nacos-OK; curl -s -u root:taosdata -d \"SHOW DATABASES\" http://127.0.0.1:6041/rest/sql | grep -o iot_tsdb_raw"'
```
Expected: `Nacos-OK` + `iot_tsdb_raw`（库已存在——说明前序探测已建，幂等分支跳过）。再验证 `stress seed` 幂等可重复：
```bash
java -jar test/stress/target/stress.jar seed --count 3 --product snd_ess_pcs --secret-base sanduo-stress --user root --password "${MYSQL_PASSWORD:-}"
```
Expected: 退出码 0，无异常（small-count 冒烟，等价于 16000 的可达性验证）。

---

### Task 2: 补四档吞吐压测 + 外部观测快照 + 判停

**Files:**
- Modify: `test/stress/run-baseline.sh`（在 seed 之后追加 §3 吞吐压测）

**Interfaces:**
- Consumes: Task 1 的 `td_sql`/`td_count`、lib.sh 的 `broker_field`/`kafka_group_lag_sum`、`stress` 工具
- Produces: `$RESULTS_DIR/tier-N.log`（stress 输出）、`$RESULTS_DIR/summary.json`（四档数据 + 判停结论）

**观测口径（每档前/后快照）：**
- Broker `messagesIn` 增量 = 设备→Broker 真实接受量（对照 stress 的 published）
- Kafka LAG 合计 = 消费积压（energy-access-uplink / energy-tsdb-prop / energy-cmd-ack）
- TDengine `count(*)` 增量 = 端到端落库量

- [ ] **Step 1: 追加四档吞吐循环 + 观测快照 + 判停逻辑**

在 seed 段之后追加（Edit `run-baseline.sh`，替换末尾 `info "[3/6] seed 完成"` 之后追加）：

```bash
# ---------------------------------------------------------------
# 3. 四档吞吐压测（4万→8万→16万→32万 msg/s；每档前后快照）
# ---------------------------------------------------------------
log "[4/6] 四档吞吐压测（每档 ${DURATION}s，判停找峰值）..."
TIERS=(2000 4000 8000 16000)   # 台数 × 20 msg/s
RATE=20
LAG_GROUPS="energy-access-uplink energy-tsdb-prop energy-cmd-ack"

snapshot() { # 输出: 当前时点各观测值（一行）
  local lag=0 g
  for g in $LAG_GROUPS; do
    lag=$((lag + $(kafka_group_lag_sum "$g" 2>/dev/null || echo 0)))
  done
  local msgin
  msgin=$(broker_field messagesIn 2>/dev/null || echo 0)
  local rows
  rows=$(td_count "iot_tsdb_raw.st_prop_snd_ess_pcs" 2>/dev/null || echo 0)
  printf "%s msgin=%s rows=%s lag=%s\n" "$(date +%H:%M:%S)" "${msgin:-0}" "${rows:-0}" "${lag:-0}"
}

prev_avg=0
peak_tier=0
summary_rows=()
for ((t = 0; t < MAX_TIERS; t++)); do
  count=${TIERS[$t]}
  tier=$((t + 1))
  log "  ── 档 ${tier}: ${count} 台 × ${RATE} msg/s = $((count * RATE)) msg/s（${DURATION}s）──"
  before="$(snapshot)"
  info "     before: ${before}"
  if ! stress throughput --count "$count" --rate "$RATE" --duration "$DURATION" \
      > "${RESULTS_DIR}/tier-${tier}.log" 2>&1; then
    info "     ⚠ 档 ${tier} stress 非零退出（见 tier-${tier}.log），继续下一档"
  fi
  after="$(snapshot)"
  info "     after : ${after}"

  # 解析 stress 输出（ThroughputLoad 汇总块）
  avg=$(grep -oE '平均吞吐[[:space:]]*: [0-9]+' "${RESULTS_DIR}/tier-${tier}.log" | grep -oE '[0-9]+' | head -1)
  p50=$(grep -oE 'P50=[0-9]+' "${RESULTS_DIR}/tier-${tier}.log" | head -1 | cut -d= -f2)
  p95=$(grep -oE 'P95=[0-9]+' "${RESULTS_DIR}/tier-${tier}.log" | head -1 | cut -d= -f2)
  p99=$(grep -oE 'P99=[0-9]+' "${RESULTS_DIR}/tier-${tier}.log" | head -1 | cut -d= -f2)
  pub=$(grep -oE '累计上报[[:space:]]*: [0-9]+' "${RESULTS_DIR}/tier-${tier}.log" | grep -oE '[0-9]+' | head -1)
  fail=$(grep -oE '发布失败[[:space:]]*: [0-9]+' "${RESULTS_DIR}/tier-${tier}.log" | grep -oE '[0-9]+' | head -1)
  avg=${avg:-0}; p50=${p50:-0}; p95=${p95:-0}; p99=${p99:-0}; pub=${pub:-0}; fail=${fail:-0}

  info "     档 ${tier} 结果: avg=${avg} P50=${p50} P95=${p95} P99=${p99} 发布=${pub} 失败=${fail}"

  # 判停：增幅 <15% 或失败率 >0.1%
  gain=100000
  if [ "$prev_avg" -gt 0 ]; then
    gain=$(awk -v a="$avg" -v p="$prev_avg" 'BEGIN{printf "%.1f", (p>0)? (a-p)*100/p : 100000}')
    info "     吞吐增幅: ${gain}%（上档 ${prev_avg}）"
  fi
  if [ "$fail" -gt 0 ] && [ "$pub" -gt 0 ]; then
    failrate=$(awk -v f="$fail" -v p="$pub" 'BEGIN{printf "%.3f", f*100/p}')
    info "     失败率: ${failrate}%"
    if [ "${failrate%.*}" -ge 1 ] || awk -v f="$failrate" 'BEGIN{exit !(f > 0.1)}'; then
      info "     ⚠ 失败率 >0.1%，判该档为峰值档，停止升档"
      peak_tier=$tier; break
    fi
  fi
  if [ "$prev_avg" -gt 0 ] && awk -v g="$gain" 'BEGIN{exit !(g < 15)}'; then
    info "     ⚠ 吞吐增幅 <15%，判该档为峰值档，停止升档"
    peak_tier=$tier; break
  fi
  prev_avg=$avg
  summary_rows+=("{\"tier\":${tier},\"count\":${count},\"rate\":${RATE},\"avg\":${avg},\"p50\":${p50},\"p95\":${p95},\"p99\":${p99},\"published\":${pub},\"failed\":${fail},\"before\":\"${before}\",\"after\":\"${after}\"}")
  if [ "$tier" -eq "$MAX_TIERS" ]; then peak_tier=$tier; fi
done

if [ "$peak_tier" -eq 0 ]; then peak_tier="$tier"; fi
info "[4/6] 峰值档: 档 ${peak_tier}（avg=${avg} msg/s）"
```

- [ ] **Step 2: 语法检查**

Run：`bash -n test/stress/run-baseline.sh`
Expected: 无输出，退出码 0。

- [ ] **Step 3: 冒烟验证单档（不跑全流程）**

直接调用 stress 跑最小档（20 台 × 20 msg/s × 5s），验证解析正则与该输出匹配：
```bash
java -Dfile.encoding=UTF-8 -jar test/stress/target/stress.jar throughput \
  --count 20 --rate 20 --duration 5 | grep -E '平均吞吐|秒级吞吐|发布失败'
```
Expected: 三行均有数字；`平均吞吐` 行匹配 `平均吞吐      : NNN msg/s` 形态。

---

### Task 3: 补演练 05/01/02 + 汇总 summary.json

**Files:**
- Modify: `test/stress/run-baseline.sh`（在判停后追加 §4 演练、§5 汇总）

**Interfaces:**
- Consumes: `test/drill/05-command-p99.sh`、`01-kafka-rebalance.sh`、`02-broker-restart.sh`（复用 lib.sh）
- Produces: `$RESULTS_DIR/summary.json`（含 P99 + 演练结果）、`control-p99.txt`

- [ ] **Step 1: 追加演练 05/01/02 + 汇总段**

在 Task 2 末尾（`info "[4/6] 峰值档..."` 之后）追加：

```bash
# ---------------------------------------------------------------
# 4. 演练 05：控制链路 P99
# ---------------------------------------------------------------
log "[5/6] 演练 05：控制链路 P99（P99 ≤ 500ms）..."
"${ROOT}/test/drill/05-command-p99.sh" > "${RESULTS_DIR}/drill-05.log" 2>&1
DRILL05_RC=$?
cat "${RESULTS_DIR}/drill-05.log"
P99_CTRL=$(grep -oE 'P99=[0-9]+' "${RESULTS_DIR}/drill-05.log" | head -1 | cut -d= -f2)
P99_CTRL=${P99_CTRL:-N/A}
info "[5/6] 控制链路 P99=${P99_CTRL}ms（drill exit=${DRILL05_RC}）"

# ---------------------------------------------------------------
# 4b. 演练 01/02（03/04 受限不执行，报告标注）
# ---------------------------------------------------------------
log "[5b/6] 演练 01：Kafka 重平衡 ..."
"${ROOT}/test/drill/01-kafka-rebalance.sh" > "${RESULTS_DIR}/drill-01.log" 2>&1
DRILL01_RC=$?
info "  演练 01 exit=${DRILL01_RC}"

log "[5c/6] 演练 02：Broker 重启自愈 ..."
"${ROOT}/test/drill/02-broker-restart.sh" > "${RESULTS_DIR}/drill-02.log" 2>&1
DRILL02_RC=$?
info "  演练 02 exit=${DRILL02_RC}"

# ---------------------------------------------------------------
# 5. 汇总 summary.json
# ---------------------------------------------------------------
log "[6/6] 写汇总 ${RESULTS_DIR}/summary.json ..."
{
  echo "{"
  echo "  \"runId\": \"${RUN_ID}\","
  echo "  \"maxTiers\": ${MAX_TIERS},"
  echo "  \"durationSec\": ${DURATION},"
  echo "  \"peakTier\": ${peak_tier},"
  echo "  \"controlP99Ms\": ${P99_CTRL},"
  echo "  \"drills\": {"
  echo "    \"05\": \"exit=${DRILL05_RC}\","
  echo "    \"01\": \"exit=${DRILL01_RC}\","
  echo "    \"02\": \"exit=${DRILL02_RC}\","
  echo "    \"03\": \"受限未执行（需 docker-redis）\","
  echo "    \"04\": \"受限未执行（需管理员停 MySQL）\""
  echo "  },"
  echo "  \"tiers\": ["
  first=1
  for row in "${summary_rows[@]:-}"; do
    [ -n "$row" ] || continue
    [ "$first" -eq 0 ] && echo ","
    printf "    %s" "$row"
    first=0
  done
  echo ""
  echo "  ]"
  echo "}"
} > "${RESULTS_DIR}/summary.json"
log "===== run-baseline 完成（${RUN_ID}）====="
```

- [ ] **Step 2: 语法检查**

Run：`bash -n test/stress/run-baseline.sh`
Expected: 无输出，退出码 0。

- [ ] **Step 3: 验证 summary.json 生成逻辑（独立小脚本段）**

用最小数据源验证 JSON 拼接（不执行真实压测）：
```bash
RESULTS_DIR=/tmp/p0-5-json-test; mkdir -p "$RESULTS_DIR"
summary_rows=('{"tier":1,"avg":123}' '{"tier":2,"avg":456}')
P99_CTRL=321; DRILL05_RC=0; DRILL01_RC=0; DRILL02_RC=0; peak_tier=2; RUN_ID=test; MAX_TIERS=4; DURATION=60
# 手动运行 Step 1 的 JSON 段（复制逻辑），然后:
python -c "import json; d=json.load(open('$RESULTS_DIR/summary.json')); print('OK', d['peakTier'], len(d['tiers']))"
```
Expected: `OK 2 2`（JSON 合法，tiers 数组长度 2）。

---

### Task 4: 执行 run-baseline.sh 全流程

**Files:**
- Execute: `test/stress/run-baseline.sh`
- Produce: `test/stress/results/<run-id>/`（seed.log、tier-{1..4}.log、drill-*.log、summary.json）

**Interfaces:**
- Consumes: 全栈运行（Nacos/Redis/MySQL/Docker Kafka/TDengine + 11 服务）；`deploy/env/local.env`
- Produces: 首组真实压测数据

- [ ] **Step 1: 确认全栈就绪**

```bash
source test/drill/lib.sh
port_open 127.0.0.1 8848 && echo Nacos-OK
port_open 127.0.0.1 9092 && echo Kafka-OK
port_open 127.0.0.1 8082 && echo BrokerMgmt-OK
port_open 127.0.0.1 6041 && echo TDengine-OK
port_open 127.0.0.1 3306 && echo MySQL-OK
port_open 127.0.0.1 6379 && echo Redis-OK
```
Expected: 全部 OK。

- [ ] **Step 2: 执行全流程**

Run（Git Bash，仓库根；后台执行，日志落盘）：
```bash
bash -c 'source deploy/env/local.env && MYSQL_PASSWORD="$MYSQL_PASSWORD" bash test/stress/run-baseline.sh' 2>&1 | tee /tmp/p0-5-run.log
```
Expected: 脚本逐段打印 `[1/6]`→`[6/6]`；四档每档 before/after 快照；`summary.json` 生成。**注意：全流程约 6-10 分钟**（seed 16000 + 4×60s 压测 + 演练 01/02）。

- [ ] **Step 3: 校验产物**

```bash
ls -la test/stress/results/$(ls -t test/stress/results/ | head -1)/
python -c "import json; d=json.load(open('test/stress/results/$(ls -t test/stress/results/ | head -1)/summary.json')); print('tiers:', len(d['tiers']), 'peak:', d['peakTier'], 'P99:', d['controlP99Ms'], 'drill05:', d['drills']['05'])"
```
Expected: 产物文件齐全；`tiers` ≥1；P99 为数字或 N/A；drill 状态为 exit=N。

- [ ] **Step 4: 交叉验证数据自洽**

```bash
python -c "
import json, glob, os
d = sorted(glob.glob('test/stress/results/*/summary.json'))[-1]
data = json.load(open(d))
for t in data['tiers']:
    print(t['tier'], 'avg=', t['avg'], 'published=', t['published'], 'failed=', t['failed'], '|', t['before'], '->', t['after'])
"
```
Expected: 每档 `avg` 与 `published/60` 数量级一致；`failed` 小；前后快照 msgin/rows 增量与 published 同量级。

---

### Task 5: 写 P0-5 压测基线报告

**Files:**
- Create: `docs/design/P0-5-压测基线报告.md`
- Modify: `docs/design/Phase9-生产化差距分析.md`（版本 bump + P0-5 行 + §5.6 落地记录）

**Interfaces:**
- Consumes: Task 4 的 `summary.json`、`tier-*.log`、`drill-*.log`；本计划 Global Constraints
- Produces: 报告文档 + Phase9 文档更新

- [ ] **Step 1: 起草报告**

创建 `docs/design/P0-5-压测基线报告.md`，结构（从 `summary.json` 读取实测值填充）：
- 版本/日期/执行环境（原生 Nacos 3.1.0 + Redis + MySQL 8.4 + Docker Kafka 4.1.2 + TDengine 3.3.1，14 核）
- **实测峰值**：四档 avg 表（2000/4000/8000/16000 台×20 msg/s），峰值档判定
- **吞吐曲线**：各档 avg msg/s、P50/P95/P99 秒级吞吐、失败率
- **端到端验证**：Broker messagesIn 增量 vs published；TDengine 落库 rows 增量 vs published（交叉验证口径）
- **控制链路 P99 基线**：演练 05 P99 值 + PASS/FAIL
- **故障演练结果**：01 Kafka 重平衡 exit、02 Broker 自愈 exit；03/04 受限未执行（标注原因）
- **瓶颈定位**：基于观测数据的瓶颈所在环节（如 tsdb 落库跟不上的证据 = rows 增量 < published、Kafka LAG 增长）
- **C-01/C-02/C-03 断言对照**：容量声明 vs 实测；P99 vs 500ms；演练执行状态
- **G-1/G-2 差距记录**（不改代码，只记录）：tsdb 不自动建库；DDL `DAYS`/`FSYNC` 与 3.3.1 不兼容需 `DURATION`/`WAL_FSYNC_PERIOD`
- **单节点环境 vs 生产容量换算**（REPLICA 1 vs 2、VGROUPS 8 vs 32、单 broker vs 集群）

- [ ] **Step 2: 更新 Phase9 文档**

Edit `docs/design/Phase9-生产化差距分析.md`：
- 版本行 `v1.4` → `v1.5（+§5.6 P0-5 首轮压测与 P99 基线）`
- P0-5 roadmap 行（~:133）标注已落地，指向 §5.6
- C-01/C-02/C-03 行（~:59-61 与 ~:335-336 两处）状态更新：`☐ 未开始` → `✅ 已执行`（附实测峰值 / P99 值 / 演练 01/02/05 状态；03/04 标注受限）
- 新增 §5.6 落地记录：四档数据表、峰值档、P99、G-1/G-2、演练结果、结论

- [ ] **Step 3: 报告自检**

Run：`grep -cE '峰值|P99|瓶颈|C-01|C-02|G-1|G-2' docs/design/P0-5-压测基线报告.md`
Expected: 输出 ≥ 6（关键词均在）。

---

### Task 6: 全量回归 + 记忆更新

**Files:**
- Verify: 全部产物与文档
- Modify: `docs/design/Phase9-生产化差距分析.md`（确认 v1.5）
- Create: memory 文件 `p0-5-loadtest-baseline.md`

- [ ] **Step 1: 全量回归验证**

```bash
bash -n test/stress/run-baseline.sh && echo "脚本语法 OK"
ls test/stress/results/*/summary.json && echo "基线数据存在"
grep -E '版本：v1.5' docs/design/Phase9-生产化差距分析.md && echo "Phase9 v1.5"
```
Expected: 全部通过。

- [ ] **Step 2: 写记忆**

Write `C:\Users\linwe\.claude\projects\D--ProgramData-Codex-Data-Energy-Storage-IoT-Platform\memory\p0-5-loadtest-baseline.md`：
- 实测峰值档、avg msg/s、P99 值、演练结果
- **G-1/G-2 差距**（tsdb 不自动建库；DDL `DAYS`/`FSYNC`→`DURATION`/`WAL_FSYNC_PERIOD`）——非衍生，是环境真相
- **lib.sh kafka_cmd 路径**：apache/kafka 官方镜像路径 `/opt/kafka/bin/`（已改，勿回退 bitnami）
- 相关 [[local-dev-environment]]、[[nacos-config-secrets]]

Update `MEMORY.md` index 加一行。

- [ ] **Step 3: 汇报总结**

向用户报告：实测峰值、P99 基线、演练结果、瓶颈定位摘要、G-1/G-2 差距、报告与文档落点。
